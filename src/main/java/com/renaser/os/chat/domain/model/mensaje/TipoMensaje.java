package com.renaser.os.chat.domain.model.mensaje;

/** Espejo del tipo Postgres `tipo_mensaje` (V1__baseline_renaser.sql:83). */
public enum TipoMensaje {
    TEXTO,
    IMAGEN,
    AUDIO,
    VIDEO,
    SISTEMA
}
