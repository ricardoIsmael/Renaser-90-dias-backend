package com.renaser.os.users.infrastructure.adapter.out.persistence.participante;

/** Espejo EXACTO del tipo Postgres `tipo_meta` (espanol). La traduccion explicita a/desde
 * {@code TipoMeta} (ingles, domain) vive en ParticipacionProgramaPersistenceMapper. */
public enum TipoMetaJpa {
    FISICA,
    VENTAS,
    MIEDO
}
