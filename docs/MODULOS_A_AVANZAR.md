# Módulos a avanzar — plan de construcción de RenaserBack (Java)

**Fecha:** 2026-08-22
**Documentos hermanos:** `CLAUDE.MD` (reglas de trabajo §0, arquitectura y convenciones, aplica a todos los módulos) · `docs/MODULO_<NOMBRE>.md` (estado, decisiones y preguntas abiertas de cada módulo — hoy solo [`MODULO_USERS.md`](MODULO_USERS.md)) · [`BITACORA_ERRORES.md`](BITACORA_ERRORES.md) (base de conocimiento de errores — **buscar ahí antes de investigar un error**)
**Alcance:** en qué orden se construyen los 14 módulos, por qué ese orden, y cuándo cada uno está terminado.

> Este documento responde **qué construir y en qué orden**.
> `CLAUDE.MD` responde **cómo construirlo** (§5.1.2 carpetas, §5.4 DTOs/validación/logs, §5.4.10 checklist de PR).
> Leer §5.1.2 de `CLAUDE.MD` antes de tocar nada.

---

## 0. Autenticación y RBAC

Decisión de fondo (Supabase Auth, no Keycloak), modelo de RBAC, defensa en profundidad, login social y la discusión cookies-vs-Bearer viven ahora en **[`docs/MODULO_USERS.md`](MODULO_USERS.md) §5** — es donde se implementan, y donde estaban generando ediciones constantes a este archivo cada vez que algo cambiaba. Los bloqueantes **B-2** (claves RS256) y **B-4** (RLS de `INSERT`) siguen siendo bloqueantes globales, se listan también en §2 de este documento.

---

## 1. Cómo se lee este plan

**Regla de orden:** un módulo se construye después de aquellos de los que depende. No se avanza a la siguiente ola hasta que la anterior esté con sus tests en verde y su `ArchitectureTest` pasando.

**Estados:** ✅ hecho · 🔄 en curso · ⬜ pendiente · 🔒 bloqueado

**"Definición de terminado" (DoD) — común a todos los módulos:**

- [ ] `domain/` plano (sin subcarpetas, §5.1.2), sin imports de Spring/JPA/Jackson
- [ ] Tests unitarios de dominio, sin Spring y sin Postgres
- [ ] Casos de uso con comando *self-validating* (§5.4.3)
- [ ] Controller tonto: sin repositorios, sin `@Transactional`, sin reglas de negocio (§5.4.6)
- [ ] DTO de salida como proyección explícita, nunca la entidad (§8)
- [ ] Migración Flyway propia en `db/migration/<modulo>/`
- [ ] Test de integración con Testcontainers
- [ ] `ArchitectureTest` en verde
- [ ] **`./mvnw clean test` ejecutado y en verde** (`CLAUDE.MD` §0.2 — no se reporta terminado sin esto)
- [ ] **Pruebas de seguridad** (`CLAUDE.MD` §0.3): rol sin permiso → 403 · usuario `SUSPENDED` → 403 · el rol no se puede inyectar por el body · todo endpoint con `@RequiresPermission` o `@PublicEndpoint`
- [ ] **Avance documentado** y decisiones nuevas en §8; errores encontrados en `docs/BITACORA_ERRORES.md`
- [ ] Contrato verificado contra `docs/API_CONTRACT.md` — la app RN no ve ninguna ruptura

---

## 2. Bloqueantes globales abiertos

| # | Bloqueante | Impacto | Dueño |
|---|---|---|---|
| ~~B-1~~ | ~~No hay JDK 25 instalado~~ | ✅ **Resuelto 2026-08-22.** Eclipse Temurin **25.0.4.1 LTS** instalado en `C:\Program Files\Eclipse Adoptium\jdk-25.0.4.101-hotspot`; `JAVA_HOME` apunta ahí a nivel usuario y máquina. `./mvnw clean test` compila con `release 25` y pasa 21/21 | Infra |
| B-2 | **Tipo de firma del JWT de Supabase sin confirmar** (§0) | Bloquea el filtro de autenticación de `users` | Login |
| ~~B-3~~ | ~~Valores reales de `TraineePhase` desconocidos~~ | ✅ **Resuelto 2026-08-24.** Los valores reales estaban en el `schema.prisma` de producción (`PHASE_1_REBIRTH`, `PHASE_2_DEVELOPMENT`, `PHASE_3_ALCHEMIST_WARRIOR`, `PHASE_4_ASCENSION`) y ya viven en el baseline como enum `fase_programa` (`FASE_1_RENACER`…`FASE_4_ASCENSION`). **`phasecontracts` queda desbloqueado** | — |
| B-4 | **RLS de `INSERT` en `public.users` sin auditar** (§5.3.6) | Agujero de escalada de privilegios que la migración no arregla sola | Login |
| B-5 | **Permisos de `MENTOR_LEAD` sin definir** (R-2, ver [`MODULO_USERS.md`](MODULO_USERS.md) §3). *R-1 y R-4 ya cerrados* | `MENTOR_LEAD` y su perfil ya existen; falta la matriz de `Permission` | Producto |

---

## 3. Orden de construcción por olas

### Ola 0 — Fundación ✅

Todo lo demás depende de esto. Sin esta ola no hay nada.

| Módulo | Estado | Qué entrega |
|---|---|---|
| `shared` | ✅ | `UserId`, `Clock` (puerto), `NotAuthorizedException`, `DomainEvent`. Declarado `@ApplicationModule(type = OPEN)` |
| **`users`** | ✅ | **Módulo 1. Ver detalle en §4.** Dominio + casos de uso + persistencia + REST construidos y auditados (2026-08-24). Queda pendiente el auth real (Resource Server contra Supabase JWT, B-2) — hoy usa `X-Actor-Id` temporal (D-29 en `MODULO_USERS.md`) |

### Ola 1 — Dominio puro, sin IA y sin infraestructura nueva ✅

Se eligen primero porque son **reglas de negocio casi puras**: máximo valor de tests unitarios, mínima superficie de riesgo, y sirven para calibrar la arquitectura con algo real antes de tocar lo complicado.

| Módulo | Depende de | Qué entrega | Nota |
|---|---|---|---|
| `points` | `users` | Puntos y ranking | ✅ 2026-08-24. **Casi todo `adapter/in/event`**: consume `HabitCompletedEvent`, `RockCompletedEvent`. Primer consumidor real de eventos Modulith. También expone `points.api.AjustarPuntosPort`, llamado síncronamente por `habits`/`rocks` (CLAUDE.MD §9.1) |
| `phasecontracts` | `users` | Pacto de Sangre, día de firma por fase | ✅ 2026-08-24 (B-3 resuelto). La regla *"en qué fase está"* vs *"cuándo le toca"* (`CONTRACT_UNLOCK_DAY`) es dominio puro, se tradujo 1:1 |
| ~~`traineeprofile`~~ | — | — | ❌ **Descartado como módulo propio (2026-08-22).** Ver abajo |

> **Resolución: `traineeprofile` NO es un módulo.** `CLAUDE.MD` §5 lo listaba como módulo aparte mientras §5.3.2 ponía `TraineeProfile` dentro de `users` — se contradecían. Queda **dentro de `users`**, por tres razones concretas:
> 1. **Es 1-a-1 con `User`**, igual que los otros cuatro perfiles. Sacar solo este rompe la simetría de §5.3.1 sin ganar nada.
> 2. **Se crea en la misma transacción** que el usuario (`ApproveAccountRequestUseCase` crea `User` + perfil + marca la solicitud, todo atómico). Partirlo en dos módulos Modulith convierte una `@Transactional` gratis en una coordinación entre módulos — el costo que §9.1 dice explícitamente que no se paga sin necesidad.
> 3. **`users` tendría que depender de `traineeprofile` para poder crear un usuario**, y `traineeprofile` de `users` para saber de quién es el perfil. Acoplamiento circular disfrazado de modularidad.
>
> **Total: 14 módulos, no 15.**

### Ola 2 — Núcleo del producto ✅

Es lo que el aprendiz usa todos los días. Se construye después de `points` para que los eventos ya tengan quien los escuche.

| Módulo | Depende de | Qué entrega | Riesgo |
|---|---|---|---|
| `habits` | `users`, `points` | Hábito, Santuario (phone-free), check-in diario | ✅ 2026-08-24. La máquina de estados `HabitStatus` (PENDING/IN_PROGRESS/COMPLETED/FAILED/EXPIRED) es dominio puro y de altísimo valor en tests. `PhoneFreeRun` va **plano en `domain/`**, no en subcarpeta |
| `rocks` | `users`, `points` | Rock, admin de rocks, modo Verdugo | ✅ 2026-08-24 |

Acá aparece el primer `@Scheduled` real (`ExpireTracksScheduler`), que reemplaza el cron actual — y con él la primera prueba de que un caso de uso se invoca desde dos adaptadores distintos (web y scheduler). Si algo de la lógica se hubiera colado en el controller, se rompe acá.

### Ola 3 — Contenido y comunicación 🔄

Módulos de menor acoplamiento entre sí; se pueden paralelizar entre varias personas.

| Módulo | Depende de | Nota |
|---|---|---|
| `notifications` | todos | ✅ 2026-08-24. **Casi todo `adapter/in/event`.** Consumidor de eventos de todo el sistema. Es el que validó que el outbox de Modulith funciona de punta a punta (ver D-37/D-38) |
| `support` | `users` | ✅ 2026-08-24 (construido junto con `users`/`points`/`phasecontracts`, fuera de orden — no bloqueaba nada) |
| `academy` | `users` | ⬜ Incluye Academia Adaptativa y Post Program |
| `community` | `users` | ⬜ Ver **Ola 4** para el feed en vivo |
| `calendar` | `users` | ⬜ |

### Ola 4 — Tiempo real ⬜

Se separa de la Ola 3 a propósito: **es el único punto donde el monolito no escala solo** (`CLAUDE.MD` §5.2.1).

| Módulo | Qué entrega | Requisito de infraestructura |
|---|---|---|
| `chat` | Chat global por WebSocket/SSE en vez de polling | **Redis Pub/Sub.** Con más de una instancia detrás del balanceador, un evento in-process nunca sale de su instancia: dos usuarios conectados a réplicas distintas no se ven |
| `community` (feed en vivo) | Empuje en vivo del feed | Mismo canal Redis |

**Regla que no se puede saltear:** el mensaje siempre se escribe **primero en Postgres**; el pub/sub es solo el empujón en vivo a quien ya está mirando la pantalla. Redis Pub/Sub es *fire-and-forget* — si una instancia está caída un instante, pierde ese mensaje del canal.

Efecto colateral que hay que resolver en esta ola: la invalidación de la caché de rol/estado de §5.3.5 también es in-process. Con N instancias, debe viajar por el mismo canal Redis, o un `SUSPENDED` tarda hasta 30s (el TTL) en tomar efecto en las otras réplicas.

### Ola 5 — IA ⬜

**Última a propósito.** Son los módulos que más ganan con Spring AI 2.0, pero también los de mayor superficie de riesgo. Se llega acá con la arquitectura ya probada por 10 módulos.

| Módulo | Depende de | Nota |
|---|---|---|
| `evidence` | `users` (`habits`/`rocks` dependen DE `evidence`, no al revés — ver corrección abajo) | Puerto único `evidence.api.RegistrarEvidenciaPort` (registrar) + `ValidacionIAPort` interno (validar, hoy `NoOpValidacionIAAdapter`). El límite de reintentos (`intentos_ia` → `REVISION_MANUAL`) es **lógica de dominio**, vive en `evidence/domain` (`Evidencia.registrarIntentoFallido`), **no** en el adapter de Gemini |
| `onboarding` | `users`, `evidence` | Validación V90/6Ps. El patrón async + polling ya está peleado en producción: **se preserva tal cual**, solo cambia la implementación interna |
| `rag` / `renasia` | `users` | `PgVectorStore` sobre el mismo Postgres. Sin base de datos nueva |

**Corrección 2026-08-25 (dependencia real, no la de este plan original):** este plan decía que `evidence` "depende de" `habits`/`rocks`/`onboarding`. Es al revés — `evidence` es quien expone el puerto (`RegistrarEvidenciaPort`) que `habits`/`rocks` LLAMAN, así que la dependencia real es `habits`→`evidence` y `rocks`→`evidence`, no `evidence`→`habits`/`rocks`. Ya construido (2026-08-25, ver `docs/MODULO_EVIDENCE.md`): el esqueleto completo de `evidence` **sin integración de IA real** (dominio, máquina de estados de validación con fallback a revisión manual, persistencia, scheduler de cola, y el cierre de las deudas RK-2/D-H6 en `rocks`/`habits`). Lo que queda de Ola 5 sin construir: `onboarding`, `rag`/`renasia`, y la integración real de IA dentro de `evidence` (`ValidacionIAPort` con Gemini/Vertex en vez de `NoOpValidacionIAAdapter`) — por eso el marcador de la ola sigue en ⬜, no se marca completa.

**Contrato que no cambia:** `POST /validate` responde `202 {status:"processing"}` de inmediato; `GET /validate?recordingId=X` consulta estado. Es el patrón correcto también en Spring — nunca bloquear el hilo de request esperando a Gemini.

**Regla de logging crítica para esta ola:** `traceId` en MDC propagado a los `@Async`. Sin eso, un flujo async + polling es imposible de correlacionar en los logs (§5.4.9).

---

## 4. Módulo 1 — `users` (la raíz)

**Por qué es el primero:** todo módulo del sistema pregunta *"¿quién es este usuario y qué puede hacer?"*. `users` es la fuente de verdad de identidad, rol y estado. Nada se puede construir encima de algo que todavía no existe.

**Estado, roles, casos de uso, preguntas abiertas (R-1 a R-5) y el registro de decisiones específicas de este módulo viven en [`docs/MODULO_USERS.md`](MODULO_USERS.md).** Ese documento se actualiza en cada avance de `users`; este archivo ya no — así se evita reescribir el plan de los 14 módulos cada vez que algo cambia en uno solo.

Resumen de una línea para quien solo necesita el mapa general: `users` ya tiene dominio, casos de uso, persistencia (verificada contra Postgres real) y una API REST construidos; falta la autenticación real (bloqueada por B-2) y la matriz fina de permisos (bloqueada por R-2).

---

## 5. Mapa de dependencias entre módulos

```
                          shared
                             │
                          users ◄──────────── todos preguntan "quien es y que puede"
                             │
        ┌────────────┬───────┴────────┬──────────────┐
        ▼            ▼                ▼              ▼
     points   phasecontracts     academy        support
        ▲            ▲           community      calendar
        │            │                │
        │            │                ▼
        │            │             chat (Redis Pub/Sub)
        │            │
     habits ─────► eventos ─────► notifications
     rocks                            ▲
        │                             │
        ▼                             │
    evidence ──► onboarding ──────────┘
        │
      rag / renasia
```

Las flechas son **dependencias de compilación** (llamada directa a la API pública de otro módulo). Los eventos de dominio van al revés y no crean acoplamiento: `habits` publica `HabitCompletedEvent` sin saber que `points` y `notifications` lo escuchan.

---

## 6. Lo que NO se construye todavía

Registrado para que nadie lo empiece por iniciativa propia:

| Pieza | Cuándo |
|---|---|
| RabbitMQ / Kafka | Solo si `evidence`/`rag` se extrae como servicio aparte (§9). Hoy los eventos in-process de Modulith cuestan nanosegundos y ya tienen outbox |
| Microservicios | Solo ante las 3 señales de §9: perfil de carga divergente, aislamiento de fallos, o ciclo de release distinto. **Separar sin necesidad cambia una `@Transactional` gratis por una Saga con compensaciones a mano** |
| GraalVM Native Image | Solo si aparece requisito de cold-start. En un monolito siempre-arriba no es prioridad |
| Migrar el storage de audios fuera de Google Drive | Queda detrás de `AudioCatalogPort`; se migra después sin tocar dominio |
| Migrar Postgres fuera de Supabase | No es problema de arquitectura, es infraestructura ya resuelta |
| Reescribir auth | Ver §0 |

---

## 7. Repositorio: stack y cómo compilar

```
renaser-backend/
├── pom.xml                    Boot 4.1.1 · Java 25 · Spring AI 2.0.0 · Modulith 2.1.0 · MapStruct 1.6.3
├── mvnw / mvnw.cmd
├── docker-compose.yml         Postgres local (pgvector) para desarrollo y Testcontainers
├── CLAUDE.MD                  arquitectura y convenciones (CÓMO), aplica a todos los módulos
└── docs/
    ├── MODULOS_A_AVANZAR.md   este documento (QUÉ y en qué orden)
    ├── MODULO_USERS.md        estado y decisiones del módulo users
    └── BITACORA_ERRORES.md    errores y bugs encontrados
```

El detalle de qué hay dentro de `src/` por módulo vive en el doc de cada módulo (`docs/MODULO_<NOMBRE>.md`), no acá — evita que este archivo quede desactualizado apenas alguien agrega una clase.

```bash
./mvnw clean test
```

**Requisito:** Eclipse Temurin JDK **25.0.4.1 LTS**. `JAVA_HOME` a nivel usuario y máquina apuntando a `C:\Program Files\Eclipse Adoptium\jdk-25.0.4.101-hotspot`. Reinstalar en otra máquina Windows: `winget install --id EclipseAdoptium.Temurin.25.JDK`.

> **Nota para quien también trabaje en Android:** si `./gradlew` de un proyecto Android toma Java 25 desde la terminal y el Android Gradle Plugin lo rechaza, fijá el JDK en el `gradle.properties` de ese proyecto con `org.gradle.java.home` en vez de tocar el `JAVA_HOME` global.

---

## 8. Registro de decisiones

Una línea por decisión cerrada, para no rediscutirlas. **Solo decisiones globales, que aplican a más de un módulo.** Las decisiones específicas de un módulo (por ejemplo, todo lo de RBAC/auth y las D-9 a D-32 de `users`) van al doc de ese módulo — ver [`docs/MODULO_USERS.md`](MODULO_USERS.md) §7. La numeración `D-N` es un solo contador compartido entre este archivo y los docs de módulo, por eso hay saltos acá (D-9 a D-15, D-18, D-19, D-21, D-22, D-24, D-25, D-27 a D-32 están en `MODULO_USERS.md`).

| # | Decisión | Fecha | Dónde está el razonamiento |
|---|---|---|---|
| D-1 | **Monolito modular + hexagonal por módulo**, no microservicios | 2026-08-21 | `CLAUDE.MD` §4.1, §9 |
| D-2 | **Maven**, no Gradle. El build tool es irrelevante para hexagonal | 2026-08-22 | `CLAUDE.MD` §5.1 |
| D-3 | **Paquete base `com.renaser.os`** (el esqueleto usaba `com.renaser.renaser_backend`) | 2026-08-22 | `CLAUDE.MD` §5.1 |
| D-4 | ~~`domain/` plano: solo clases, sin subcarpetas~~ **matizado por D-26**: plano si es un solo agregado, subcarpeta por agregado si hay varios | 2026-08-22 | `CLAUDE.MD` §5.1.2 |
| D-5 | **MapStruct solo en persistencia**; mapeo a mano en la frontera web | 2026-08-22 | `CLAUDE.MD` §5.4.5 |
| D-6 | **Validación en tres niveles**: DTO web → comando self-validating → dominio | 2026-08-22 | `CLAUDE.MD` §5.4.3 |
| D-7 | **Controller tonto**: sin repositorios, sin `@Transactional`, sin reglas | 2026-08-22 | `CLAUDE.MD` §5.4.6 |
| D-8 | **`domain/` no loguea** | 2026-08-22 | `CLAUDE.MD` §5.4.9 |
| D-16 | **`traineeprofile` no es un módulo**: vive dentro de `users`. Total 14 módulos | 2026-08-22 | §3, Ola 1 |
| D-17 | **JDK: Eclipse Temurin 25 LTS.** `JAVA_HOME` de usuario movido del JBR de Android Studio a Temurin | 2026-08-22 | §7 |
| D-20 | **Reglas de trabajo obligatorias** en `CLAUDE.MD` §0: sin atribución de IA en commits, pruebas siempre, todo se documenta, bitácora de errores | 2026-08-22 | `CLAUDE.MD` §0 |
| D-23 | **`CLAUDE.MD` §5.4.5 corregido: Lombok SÍ se usa en `domain/`** (`@Getter`, `@AllArgsConstructor(PRIVATE)`, `@EqualsAndHashCode(of=...)`, `@Accessors(fluent=true)`), solo `@Data`/`@Setter`/`@ToString` sin acotar siguen prohibidos. Verificado en vivo contra `thombergs/buckpal` (`Account.java` usa exactamente ese patrón). La regla anterior ("prohibido en domain/") era más estricta que su propia fuente citada | 2026-08-24 | `CLAUDE.MD` §5.4.5 |
| D-33 | **La BD nueva es el baseline único y el análisis va primero.** `src/main/resources/db/migration/V1__baseline_renaser.sql` (90 tablas, esquema `renaser`, español) es la **fuente operativa** de la BD; el diseño de referencia y su auditoría viven en `docs/db/` (93 tablas — las 3 de diferencia son los perfiles fusionados/diferidos por D-25, y las tablas RBAC retenidas [SUPERADO] por D-21 hasta ajustar `roles_permitidos_curso`/`roles_destino_evento` en academy/calendar). Además, **cada módulo arranca con un "paso 0" de análisis del código viejo** de su feature (patrón `reminders.ts`: extraer reglas exactas antes de codificar, pedido de Luis/Ricardo) | 2026-08-24 | `docs/db/AUDITORIA_REDISENO_BD.md`, este §3 |
| D-36 | **Vocabulario del wire: el contrato de la app habla el idioma VIEJO (inglés)**. La BD nueva usa enums en español (`FASE_2_DESARROLLO`, `ABIERTO`…) pero los DTOs REST traducen a los literales que la app instalada ya consume (`PHASE_2_DEVELOPMENT`, `OPEN`…) — la traducción vive en la frontera web de cada módulo, nunca en dominio ni persistencia. `support` ya lo hace; es regla para todos | 2026-08-24 | Reportes Lote 1; `CLAUDE.MD` §8/§10 (no romper el contrato) |
| D-34 | **Storage de archivos: AWS S3 real** (elegido sobre Supabase-S3 y MinIO). Puerto transversal `AlmacenamientoPort` en `shared` + `S3StorageAdapter` (SDK v2); la app deja de subir directo (URLs prefirmadas vía API); columnas `bucket`+`ruta_storage` ya agnósticas; migración de objetos por módulo. Audios Espíritu siguen en Drive detrás de su puerto. **El adaptador real se construyó el 2026-08-31 — ver D-54**, que es donde están las condiciones para activarlo y los permisos IAM mínimos | 2026-08-24 | `docs/PLAN_DE_MODULOS.md` |
| D-35 | **Plan de desarrollo por módulo** en [`docs/PLAN_DE_MODULOS.md`](PLAN_DE_MODULOS.md): 13 módulos restantes + cierre de `users`, en lotes de 3 con agentes supervisados; al arrancar cada módulo se crea su `docs/MODULO_<NOMBRE>.md` con su sección como semilla | 2026-08-24 | `docs/PLAN_DE_MODULOS.md` |
| D-26 | **`domain/` corregido: subcarpeta por agregado, no siempre plano.** La regla anterior ("domain/ nunca subcarpetas") se basaba en una sola referencia (`buckpal`) que tiene un solo agregado. Verificado contra `citerus/dddsample-core` (múltiples agregados: cargo/handling/location/voyage, cada uno en su carpeta): cuando un módulo tiene **más de un agregado independiente** (identidad propia, ciclo de vida propio, repositorio propio), cada uno va en su subcarpeta. `users/domain/` tenía 3: `User`, `AccountRequest`, `MentorProfile` — pasaron a `user/`, `accountrequest/`, `mentorprofile/`. Sigue prohibido subdividir por capa (`domain/entities/`, etc.) | 2026-08-24 | `CLAUDE.MD` §5.1.2 |
| D-37 | **La tabla `event_publication` del outbox de Spring Modulith se crea a mano, vía Flyway.** `spring-modulith-starter-jpa` no trae ningún script de schema propio (verificado: el jar no contiene un solo `.sql`) — es responsabilidad del proyecto. Se agregó `V2__spring_modulith_event_publication.sql` con el DDL derivado del mapeo JPA real de `JpaEventPublication` (`ddl-auto=update` contra Testcontainers, volcado de `information_schema`, no inventado). Regla para cualquier módulo que publique eventos: la migración ya existe, no crearla de nuevo | 2026-08-24 | E-28 en `docs/BITACORA_ERRORES.md` |
| D-38 | **Los tests de integración en este repo terminan en `*Test.java`, nunca `*IT.java`.** El proyecto usa solo Surefire (fase `test`), sin Failsafe — Surefire ignora en silencio cualquier archivo que no matchee `**/*Test.java`/`**/Test*.java`/`**/*Tests.java`/`**/*TestCase.java`, y `mvn test` reporta éxito sin haber corrido nada. Pasó con el E2E del outbox de `notifications` (`NotificationsEventOutboxIT` → renombrado a `NotificationsEventOutboxTest`) | 2026-08-24 | E-27 en `docs/BITACORA_ERRORES.md` |
| D-39 | **`application.yaml` (main) excluye las auto-configuraciones de Spring AI Google GenAI** (chat/embedding/pgvector) hasta que `evidence`/`onboarding`/`rag` (Ola 5) se construyan con credenciales reales de Gemini/Vertex — sin esto la app no arranca fuera de los tests (que ya tenían el mismo exclude desde antes, ver E-15). Quitar el exclude es parte del trabajo de esos módulos, no antes | 2026-08-24 | E-15/E-28 en `docs/BITACORA_ERRORES.md`, `src/main/resources/application.yaml` |
| D-40 | ~~Convertir `roles_permitidos_curso.rol_id`/`roles_destino_evento.rol_id` a columna enum nativa~~ **ANULADA el mismo día, antes de escribir una sola línea de SQL.** Decisión del dueño del proyecto: **la BD queda cerrada en el baseline de 90 tablas y no se toca** — ni tablas nuevas, ni ALTER, ni seeds. Las dos junction se usan tal cual (`rol_id smallint REFERENCES roles(id)`); la traducción a `users.api.UserRole` la hace el adaptador de persistencia leyendo las 5 filas de `roles` (cacheadas). El pendiente que D-21 dejó abierto se resuelve así, en código, no en esquema. **Regla general que queda: ningún módulo crea migraciones Flyway**; los catálogos (`categorias_muro`, `niveles_membresia`, cursos…) llegan en la fase posterior de migración de datos desde producción, y todo módulo debe funcionar con su tabla vacía | 2026-08-24 | Este §8; instrucción directa de Luis/Ricardo |
| D-41 | **La app NO habla con Postgres: todo pasa por la API de Spring.** Decision de Luis/Ricardo (2026-08-24): depender de Supabase para datos es mala practica. Hoy la app hace **37 accesos directos** a tablas (+2 RPC), de los cuales **9 son ESCRITURAS** — eso pone la regla de negocio en el cliente y deja la unica defensa en manos de RLS (el agujero de B-4). Cada acceso directo se reemplaza por un endpoint. **Alcance acotado a acceso a datos**: Supabase sigue siendo el proveedor de identidad (emite el JWT que Spring valida como Resource Server, D-1 de `MODULO_USERS.md`) y el hosting del Postgres — ambas cosas se pueden cambiar despues sin rehacer este trabajo. El storage ya salio de Supabase por D-34 (AWS S3). **Regla para todo modulo nuevo:** si la app hace hoy un `.from(tabla)` o un `.rpc()` contra algo de tu modulo, tu modulo debe exponer el endpoint equivalente — se releva en el paso 0 (D-33) | 2026-08-24 | Este §8; auditoria de la app RN en `docs/MODULOS_A_AVANZAR.md` §9 |
| D-42 | **El panel admin en Next.js no frena nada.** Se sigue construyendo la API completa; el cambio del panel se hace al final, cuando todo este listo. No se monta proxy de transicion ni se coordina release por modulo mientras tanto | 2026-08-24 | Instruccion directa de Luis/Ricardo |
| D-43 | **El ranking general se calcula con consultas EN LOTE por modulo, no con un procedimiento almacenado.** El backend viejo resolvio este calculo con la funcion `general_ranking_scores()` (`prisma/migrations/general_ranking_scores_function.sql`) por un incidente real y medido: el calculo hacia **una consulta por aprendiz** para el progreso de cursos y con ~30 cuentas activas devolvia *"Too many database connections opened"* (verificado en vivo el 2026-08-12). El problema era el **N+1**, no el volumen — y se resuelve igual con consultas en lote, sin mover regla de negocio a SQL. Decision (Luis/Ricardo, 2026-08-24): cada modulo expone su porcentaje EN LOTE por su `api/` (`habits` → %habitos, `rocks` → %rocas, `academy` → %cursos) y `points` combina con los pesos **50% habitos + 35% rocas + 15% cursos** en su DOMINIO, testeable sin Postgres. Tres consultas totales, y el snapshot corre de noche. **Descartado a proposito:** portar la funcion a Flyway (la formula quedaria en SQL y una sola funcion leeria tablas de 4 modulos, saltandose todos sus limites de una vez) y meter el mismo SQL como query nativa en `points` (mismo problema de limites, sin la ventaja de estar en la base). La formula se porta LITERAL de `src/lib/coherence.ts::averageCompletionForDates` y `cursos/repository.ts::sumarProgresoCursos` — no se recalibra ningun criterio | 2026-08-24 | Este §8; cabecera de `general_ranking_scores_function.sql` en el repo viejo |
| D-44 | **Un modulo sin eventos previos SI puede sumar un DomainEvent nuevo cuando otro modulo lo necesita para reaccionar** (patron confirmado con `chat`, Ola 4): se agrego `users.api.UsuarioRegistradoEvent` (publicado desde `AccountRequestService.approve` y `UserAccountService.invite`) y `community.api.CelulaCreadaEvent` (publicado desde `CelulaService.crear`) para que `chat` pudiera auto-unir usuarios a la conversacion GLOBAL y auto-crear la conversacion de una celula, sin tocar ninguna otra logica de `users`/`community`. Regla para el proximo modulo que necesite lo mismo: agregar el evento al `api/` del modulo dueno del dato, publicarlo con `ApplicationEventPublisher` DENTRO de la misma transaccion que ya escribe el agregado, y actualizar solo el test unitario del servicio dueno para mockear el publisher nuevo — nunca reescribir la logica de negocio existente para "hacer lugar" al evento | 2026-08-25 | `docs/MODULO_CHAT.md` §2, §4 |
| D-45 | **Envio de correo real por SMTP, y el switch real/NoOp por propiedad explicita.** El flujo de alta quedo sin salida cuando se solto Supabase: solo existia `NoOpEnviarEmailAdapter`, asi que ni el codigo de verificacion ni el link de activacion llegaban a nadie fuera de la maquina del desarrollador. Se agrego `spring-boot-starter-mail` + `SmtpEnviarEmailAdapter` sobre `JavaMailSender` (autoconfigurado por `spring.mail.*`, Boot 4.1). **Cual adaptador se usa lo decide `renaser.email.proveedor` (`smtp` \| `noop`, por defecto `noop`)** y NO `@ConditionalOnBean(JavaMailSender.class)`: `@ConditionalOnBean` depende del orden de registro de beans y Spring solo lo garantiza dentro de autoconfiguracion, no en clases escaneadas. Con dos `@ConditionalOnProperty` simetricas sobre la misma propiedad siempre hay exactamente un `EnviarEmailPort`. La redaccion vive en `PlantillasEmail` (SRP): el adaptador solo transporta. Un fallo del proveedor lanza `EnvioEmailFallidoException` → **503**, nunca se traga — tragarlo dejaria cuentas aprobadas que nadie puede usar ni descubrir. **Pendiente del usuario:** credenciales SMTP reales (`SMTP_HOST`/`SMTP_USERNAME`/`SMTP_PASSWORD`) y dominio remitente | 2026-08-27 | [Spring Boot 4.1 — Sending Email](https://docs.spring.io/spring-boot/reference/io/email.html), [Spring Framework 7.0 — Email](https://docs.spring.io/spring-framework/reference/integration/email.html) |
| D-46 | **Los tres endpoints publicos de consulta de correo se portan del repo viejo, y el limite de tasa se mueve del borde a la aplicacion.** `POST /account-requests/check-email`, `/exists` y `/verify-email` existian en el contrato que la app ya consume pero no se habian migrado (devolvian 405). Se portan con sus reglas literales (AR-04/05/06): las dos primeras comparten un unico lookup (`ConsultarEmailRegistradoUseCase`) con dos respuestas de nombre invertido, y la tercera consulta MX por DNS (`ResolverMxPort` + `DnsResolverMxAdapter` sobre JNDI del JDK, sin dependencia nueva), con tres estados — nunca convierte un DNS caido en un "no". **Sobre enumeracion de correos:** se mantiene la decision de producto del 2026-08-01 (el dato ya se filtraba por el 409 de `POST /account-requests`; se acepta el coste a cambio de no hacer llenar seis campos para descubrir el duplicado al final). **Lo que si cambia:** el repo viejo concluyo que el limite por IP tenia que vivir en el borde (WAF) *porque en serverless no habia donde contar*; corriendo siempre-arriba y con Redis ya en el stack, vive en `ConsultaEmailService` reutilizando `LimitarSolicitudesResetPort` — que es lo que CLAUDE.MD §5.3.6 anticipaba. Umbral 120/hora por IP: mas alto que el de envio de codigos (alli cada intento cuesta un correo real; aca una lectura por indice UNIQUE) — **asuncion, no confirmada por producto**, mismo criterio que A-5 | 2026-08-27 | [OWASP — Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html), `service.ts::checkEmailAvailability` del repo viejo |
| D-47 | **Arranca la "fase posterior de migracion de datos desde produccion" que D-40 dejo anunciada, acotada a catalogo/logica — nunca datos de usuario.** Instruccion directa de Ricardo (2026-08-28): se le avisó a los usuarios que el sistema se reconstruye, asi que no hay historial de progreso que preservar; solo se migra lo que es catalogo (reglas de negocio con forma de fila: habitos default, horarios, guias, audios). Se dumpeo produccion completa (`supabase db dump --data-only`, proyecto `qchpxyaiipghayyfmthg`) a `docs/db/migracion/datos_origen/` (gitignored, tiene PII real — nunca commitear) y de ahi se escribieron `V4__catalogo_habitos_default.sql` (18 habitos de sistema activos + sus 23→18 horarios) y `V5__guias_audios_habitos_default.sql` (17 guias, 2 adjuntos, 43 audios de Espiritu, 13 audioterapias), con perfilado explicito antes de cargar (fuentes: Quinnox "Data Migration Checklist 2026", digna.ai "Data Validation During Migrations" — ver cabecera de V5). **Decisiones de limpieza tomadas, no solo copiado:** de 27 habitos en produccion se excluyeron 9 inactivos (sin historial que preservar, ya no hay razon para conservar filas muertas); eso dejo huerfanas 5 de las 22 `habit_guides` y 1 de las 3 `habit_guide_attachments` (referenciaban a los habitos excluidos), tambien excluidas — cargarlas habria violado la FK contra `habitos`. Se encontro y corrigio una inconsistencia real del dato viejo (habito CHECKBOX con `journal_entry_type` de JOURNALING cargado, que el CHECK `diario_solo_journaling` de la BD nueva rechaza) dejando ese campo en NULL en vez de inventar un valor. Los ids uuid de produccion se preservan (regla ya fijada en D-40/AUDITORIA_REDISENO_BD.md §14.8) para que una futura migracion de datos de usuario (si el negocio decide traer algo puntual) pueda referenciar estos catalogos por FK sin remapeo. **Esto NO contradice D-40**: D-40 prohibe que un modulo cree migraciones Flyway *durante su construccion inicial*; esta es exactamente la fase posterior que D-40 ya preveia para los catalogos | 2026-08-28 | Este §8; `docs/BITACORA_ERRORES.md` E-47; `docs/db/AUDITORIA_REDISENO_BD.md` §14.8 |
| D-48 | **Continuacion de D-47: se migro el catalogo de `academy` (cursos) y dos catalogos chicos de comunidad/membresia, y se dejaron explicitamente AFUERA varias tablas del mismo dump — con motivo, no por omision.** `V6__catalogo_academy_default.sql`: 23 `cursos`, 171 `secciones_curso`, 473 `lecciones`, 18 `recursos_leccion` — generado programaticamente (no transcrito a mano, ver E-47) por el volumen de HTML/markdown; 0 huerfanas, 0 duplicados, 0 violaciones del CHECK `video_coherente`. ~~`V7__catalogo_comunidad_membresia_default.sql`: 3 `niveles_membresia` + 5 `categorias_muro`~~ **RETIRADA el mismo dia, antes de aplicarse:** al hacer el backup previo de la BD local (`localhost:5433/renaser`, la que ya corre el baseline de 90 tablas) se encontro que `categorias_muro`/`niveles_membresia` **ya estaban cargadas ahi con contenido real y distinto** al del dump viejo de Supabase — `niveles_membresia` con nombres "Iniciado/Constante/Guerrero" (terminologia de marca, coherente con el resto del dominio) en vez de "Sin acceso/Nivel medio/Nivel alto"; `categorias_muro` con las mismas 5 claves pero con matices en el rotulo. Insertar V7 tal cual habria roto por PK duplicada (`categorias_muro.clave`) y por `UNIQUE(rango)` en `niveles_membresia`. En vez de forzar el insert o pisar valores ya decididos, se **retiro el archivo completo** sin haberlo aplicado nunca (no estaba en ningun `flyway_schema_history`, ni local ni de test) — la BD local ya tiene la version correcta de estos dos catalogos, de otra fuente que no es este dump. **Leccion:** antes de escribir una migracion de seed con valores fijos, revisar el estado real de la BD destino, no asumir que esta vacia solo porque el dump de origen no la tenia en el mismo formato. **Excluido, con razon:** `curso_asignaciones` (23 filas) es dato de usuario real (asigna curso a `user_id`), no catalogo — fuera de alcance de D-47; `roles_permitidos_curso` no tiene dato de origen (la columna vieja viene NULL en las 23 filas); los 4 `calendar_events` de origen son datos de prueba del propio equipo ("prueba", "Fjdndj", "Gg") — no eventos institucionales reales, no se migran; el cluster `unidades`/`perfiles`/`colaboradores`/`aperturas_diarias`/`campanas`/`contratos`/`evaluaciones_legajo`/`legajos`/`pagos_nomina`/`parametros`/`permisos_ausencias` (vocabulario de RRHH/nomina, ajeno al dominio Renaser) esta **completamente vacio** en el dump — nada que migrar, se deja constancia por si el dueño del proyecto reconoce esas tablas de otra aplicacion compartiendo el mismo proyecto Supabase. `knowledge_base` (RAG, `base_conocimiento` en el esquema nuevo) se difiere a proposito: mezcla contenido derivado de tickets reales de soporte (dato de usuario) con contenido de lecciones, y sus embeddings fueron generados con un modelo que puede no coincidir con el que use Spring AI cuando se construya `rag` (Ola 5) — migrarlos ahora arriesga vectores mudos o incompatibles. **Confirmado por el dueño del proyecto (2026-08-28): esta tabla no se migra nunca, ni siquiera cuando se construya `rag`** — el contenido de la base de conocimiento lo va a proveer el cliente subiendo archivos nuevos directamente a esa fecha, así que el `knowledge_base` viejo queda descartado por completo, no solo pospuesto. `rag` (Ola 5) arranca con la tabla vacía y un flujo de carga de documentos nuevo, no con una migración de este dato. `onboarding_sections`/`onboarding_questions` (catalogo real, con contenido) quedan **detectadas pero no migradas todavia** — no las pidio este pase, se listan como candidato para la proxima (resuelto en D-52, con `las_90_variables` excluido a proposito). **Analisis de logica propia de habitos pedido explicitamente:** `PASTILLA_RENACER` ya tiene dominio construido (`EspirituService`, `AudioCatalogPort`) pero su unico adapter es `NoOpAudioCatalogAdapter` — decision previa y documentada de NO integrar Google Drive todavia (javadoc de la clase); con los 43 `audios_espiritu` ya cargados en V5, el catalogo tiene datos pero el adapter los ignora a proposito, así que el hábito sigue sin devolver audio hasta que se conecte un adapter real (Drive o uno que lea directo de `audios_espiritu`, ya que la tabla existe). `AUDIO_THERAPY_WEEKLY` (hábito Audioterapia Semanal) **no tenia ninguna logica de dominio en el backend** (corregido: los 5 nombres PHONE_FREE_DAY/PASTILLA_RENACER/DAILY_CLASS/GREEN_JUICE/WARM_LEMON_WATER solo estaban citados en un javadoc describiendo el backend VIEJO, `PoliticaHabito.java:9-10` — el unico `implements PoliticaHabito` real en `src/` es `PoliticaSantuario`; cada clave se resuelve hoy con su propio servicio especifico, no con el mecanismo de politicas) — no habia `SelectorHabito`/servicio que vincule el dia de programa con la semana de `audioterapias`. Resuelto en D-49 | 2026-08-28 | Este §8; `docs/MODULO_HABITS.md`; `AudioCatalogPort.java`/`NoOpAudioCatalogAdapter.java`; `PoliticaHabito.java` |
| D-49 | **Se construyo el resolver de Audioterapia Semanal que D-48 dejo pendiente, con la duracion por semana configurable por endpoint (no hardcodeada) — pedido explicito del dueño del proyecto (2026-08-28): "el cliente es muy atrevido y cambia".** Confirmado con el dueño: el habito arranca dia 11, cada audioterapia dura 7 dias antes de pasar a la siguiente. El dia de inicio NO se duplica en código — se lee en vivo de `horarios_habito.dia_inicio` (ya editable desde `HorarioHabitoAdminController`, hueco #11); la duracion si es nueva: columna `audioterapias.duracion_dias` (V8, default 7, no tabla nueva) editable por semana individual via `PATCH /api/v1/admin/audio-therapies/{semana}`. El calculo recorre el catalogo sumando duraciones acumuladas desde el dia de inicio real (soporta semanas de duracion distinta, no una formula de division fija). Sin maquina de estados (a diferencia de "Espiritu"): la finalización real del habito JOURNALING ya funciona por el camino generico de `RegistroService.completar` (confirmado: no requiere `EntradaDiario`, `entrada_diario_id` queda null tal como hoy). Nuevo: `AudioterapiaCatalogPort`/`SaveAudioterapiaPort` (out), `ConsultarAudioterapiaSemanalUseCase`/`ActualizarDuracionAudioterapiaUseCase` (in), `AudioterapiaService`/`AudioterapiaAdminService` (reutiliza `HabitoAdminGuard` ya existente), `AudioTherapyController`/`AudioTherapyAdminController`. **Hallazgo colateral, no resuelto, pedido aparte si es real:** se buscó el mecanismo de "firma términos → espera 3-7 días → recién arranca el día 1 de programa" y no se encontró código que lo implemente — `participantes_programa.fecha_inicio`/`programa_activado_en` existen pero ningún caso de uso calcula esa demora automática; no bloquea este resolver porque consume `diaPrograma` tal como lo entregue `ConsultarProgresoParticipanteHabitsPort`, sea cual sea el mecanismo que lo calculó. Probado en vivo contra la BD local: cambiar la duración de la semana 1 de 7 a 10 días por el endpoint admin corrió `diaSiguienteCambio` de 18 a 21 sin tocar código. `./mvnw clean test`: 1665 tests, 0 fallos | 2026-08-28 | Este §8; plan aprobado en sesión; `EspirituService.java` como plantilla arquitectónica |
| D-50 | **Migración de binarios a S3 (cursos + audioterapias) hecha y verificada; se confirmaron los rangos reales de `fase_programa` por día, y se detectó que la fase NUNCA avanza sola en el backend — queda documentado, no resuelto todavía por decisión explícita del dueño.** `s3-renaser90dias` estaba vacío (bucket creado, sin objetos) y `AlmacenamientoPort` solo tiene `NoOpAlmacenamientoAdapter` — ningún `S3StorageAdapter` real (D-34 sigue sin construirse). Se migraron igual los binarios reales desde el Storage viejo de Supabase (`supabase storage cp` → `aws s3 sync`, perfil `renaser`, cuenta `526338061654`): 37 objetos de `cursos-media` (23 portadas + 14 assets embebidos en `cuerpo_html`, sin fila propia en la BD) y 13 `.mp3` de `audio-terapia`, **cruzados 1 a 1 contra `cursos.portada_ruta`/`audioterapias.ruta_storage` con `comm` — cero faltantes, cero sobrantes** (se detectó y corrigió en el momento un prefijo `audio-terapia/` mal puesto en la primera subida, que no coincidía con la ruta guardada en la BD). ~~Los archivos están listos, pero **no van a verse hasta que se construya el `S3StorageAdapter` real**~~ — **resuelto el 2026-08-31 en D-54**: existe `S3AlmacenamientoAdapter` y, con `STORAGE_PROVEEDOR=s3` y credenciales resolubles por el SDK, estos objetos ya se sirven por URL prefirmada. **Confirmado por el dueño del proyecto (2026-08-28) — corrige un dato que el propio código viejo marca como calendario obsoleto (22/45/67, ya no vigente):** los rangos reales de `fase_programa` por día de programa son **Fase I (Renacimiento) 1-7, Fase II (Desarrollo) 8-34, Fase III (Guerrero Alquimista) 35-64, Fase IV (Ascensión) 65-90** — coinciden con los 4 valores ya existentes en el enum. **Hallazgo:** `ParticipacionPrograma.avanzarDia()` (cron nocturno) y `.fijarDia()` (ajuste manual admin) tocan `diaPrograma` pero **ninguno de los dos actualiza `fase`** — queda fija en `FASE_1_RENACER` (el default) para siempre, salvo que alguien la cambie a mano en otro lugar no encontrado. **Decisión explícita del dueño: NO se construye el avance automático todavía** — queda documentado con los rangos ya confirmados para cuando se decida hacerlo, no se toca código de `users` en esta pasada. También confirmado: se deja `roles_permitidos_curso` **sin filas** para los 6 cursos con `acceso=RESTRINGIDO` sin `dia_desbloqueo` (RENASER 7 DIAS, RENASER 30 DIAS, PROGRAMA 90D, Sesiones de Q&A, CERTIFICACIÓN RENASER, MENTORES RENASER) — el dato real de qué rol ve cada uno no está confirmado, no se inventa (CLAUDE.MD §0.6). **Corregido 2026-08-28, verificado probando el endpoint en vivo con un actor ADMIN real:** la frase anterior decía "quedan visibles solo para ADMIN/ALCHEMIST hasta que se confirme" — eso es **incorrecto**, no coincide con el código. `Curso.visibleEnCatalogoPara` (línea 77: `return publicado && acceso == ABIERTO`) devuelve `false` para `RESTRINGIDO` **para cualquier rol**, incluido ADMIN/ALCHEMIST, salvo que `rolesPermitidos` liste ese rol explícitamente — y hoy la lista está vacía. No es un bug de esta migración: es el comportamiento **ya documentado como AC-02** en `docs/MODULO_ACADEMY.md` §5/§6, replicado fiel del `puedeVerCurso` del backend viejo (`cursos/repository.ts:731-747`), con pregunta abierta sin resolver: ¿es intencional que `RESTRINGIDO` nunca sea visible por catálogo para nadie, o es una regresión heredada? Hasta que el dueño del proyecto responda esa pregunta, estos 6 cursos no los ve ningún rol vía `GET /api/v1/cursos/{id}` — ni el aprendiz, ni el admin. **Reordenado el mismo día** (pedido explícito, "para ser buenas prácticas"): el bucket tenía los 23 folders de curso y los 13 mp3 sueltos en la raíz — se movieron a `cursos/{cursoId}/...` y `audioterapias/{archivo}` respectivamente (`aws s3 mv --recursive`), y se actualizaron `cursos.portada_ruta`/`audioterapias.ruta_storage` con el prefijo nuevo en la misma operación. Re-verificado con el mismo cruce `comm` — cero desajustes — y probado en vivo contra los dos endpoints (`/audio-therapy/status`, `/cursos`) sin reiniciar la app. **Nota para no confundir a futuro:** no hay ni puede haber un prefijo `espiritu/` en este bucket — los audios de Pastilla Renacer nunca estuvieron en S3, siguen en Google Drive por decisión ya tomada (D-34), referenciados por `drive_file_id` | 2026-08-28 | Este §8; `ParticipacionPrograma.java`; confirmación directa del dueño del proyecto |
| D-51 | **Se agregaron los 9 hábitos inactivos que D-47 había excluido — pedido explícito del dueño ("ponlos por si acaso pero no rompas nada").** `V9__catalogo_habitos_inactivos.sql`: mismas 9 filas de `docs/MODULOS_A_AVANZAR.md` D-47/D-48, cargadas con `activo=false` (baja lógica, no un alta activa) y sus 5 horarios reales (los otros 4 no tenían horario en origen, no se inventó uno). Verificado antes de escribir el archivo: 0 colisiones de id/título/clave_sistema contra los 22 hábitos de sistema ya existentes en la BD local; la única con `journal_entry_type` (REGISTRO DE INTOXICACIÓN CONSCIENTE) es tipo JOURNALING de verdad, no repite la anomalía de E-47. `./mvnw clean test`: 1665/1665. Probado en vivo: `GET /api/v1/admin/habits` devuelve 31 hábitos de sistema (22 activos + 9 inactivos), coincide exacto con lo esperado | 2026-08-28 | Este §8; `docs/BITACORA_ERRORES.md` E-47 (mismo metodo de generacion programatica) |
| D-52 | **Se migró el catálogo de onboarding que D-48 había dejado "detectado pero no migrado" — con un flujo completo excluido a pedido explícito del dueño.** `V10__catalogo_onboarding_default.sql`: 18 `secciones_onboarding` (de 27 en origen), 102 `preguntas_onboarding` (de 192) y 23 `opciones_pregunta`, en los flujos `terminos`/`pacto`/`ficha_inicial`/`cuestionario_profundo`/`diseno_destino`/`cierre_dia_1`. Generado programáticamente (mismo método que V6/V9: parser de tuplas del dump → JSON → script de mapeo con auto-validación de conteo de columnas) por el volumen. Mapeo de `question_type` a `tipo_pregunta_onboarding` 1:1 confirmado contra el propio comentario del enum en el schema nuevo. **`seccion_id`/`pregunta_id` son `IDENTITY`: se resolvieron con `INSERT..SELECT..JOIN` por clave natural** (`(flujo,clave_seccion)` y `clave_pregunta` respectivamente), nunca asumiendo que el id autogenerado coincide con el orden de inserción. **Excluido a propósito: el flujo `las_90_variables` (90 de las 192 preguntas, el 47% del catálogo origen)**, pese a que sí tiene uso real medido (221 grabaciones de audio de 17 usuarios distintos en `variables_90_recordings`, cubriendo las 90 claves — hallazgo que corrigió una conclusión mía anterior y equivocada de "no tiene uso", ver bitácora). La exclusión es **a pedido directo del dueño del proyecto** (2026-08-28): el cliente móvil tiene esa pantalla de 90 pasos hardcodeada y no lee `preguntas_onboarding` para armarla, solo sube la grabación de audio final a `variables_90_recordings` — cargar esas 90 filas sería catálogo que el cliente actual nunca consulta. **No verificado por mí de forma independiente** (no tengo acceso al repo de `RenaserPlayStore`); si el cliente cambia a leer el catálogo para esa pantalla, es una migración aparte, no una corrección de esta. Verificado antes de cargar: 0 preguntas huérfanas, 0 `clave_pregunta` duplicadas, 0 uso de `parent_question_key`/`validation_rules` (no hizo falta resolver condicionales ni reglas jsonb) en el alcance migrado. `./mvnw clean test`: 1665/1665. Probado en vivo contra `GET /api/v1/onboarding/questionnaire?flow=X` (endpoint ya existente, `CuestionarioController`): `terminos` devuelve sus 2 preguntas (casilla + firma), `ficha_inicial` devuelve 62 preguntas con las 4 escalas y las 5 `SELECCION_UNICA` con sus opciones completas, `las_90_variables` devuelve `sections:[]` tal como se espera de la exclusión | 2026-08-28 | Este §8; `docs/MODULO_ONBOARDING.md` (decisión D-O9, catálogo dejado vacío a propósito para esta fase); `docs/BITACORA_ERRORES.md` |
| D-53 | **Se sacó el último rastro de Supabase Auth del backend, incluida la BD — decisión explícita del dueño del proyecto (2026-08-31) de ir al alcance completo, no solo al cosmético.** Cierra la propagación de la decisión de auth del 2026-08-26 (`docs/MODULO_AUTH.md`), que hasta hoy vivía solo en ese documento. **Código muerto eliminado:** `SupabaseAdminAuthPort` y `NoOpSupabaseAdminAuthAdapter` — registraban un `afterCommit` para borrar el usuario en Supabase Auth al rechazar una solicitud; el borrado real siempre fue `deleteUserPort.deleteById` contra nuestro Postgres, y el puerto lo servía un adaptador que solo logueaba un `WARN`. Retirarlo no cambia comportamiento. **Renombre `supabaseUserId` → `usuarioId`** en dominio (`AccountRequest`), puerto in (`InviteAndCreateUserUseCase`), servicios, entidad JPA, mapper y el body de `POST /api/v1/users/invite`. **`V11__solicitudes_cuenta_renombrar_usuario_id.sql`**: `RENAME COLUMN supabase_user_id TO usuario_id`. **Toca una BD declarada congelada (D-40) a propósito y con criterio:** un `RENAME COLUMN` en Postgres es operación de catálogo — no reescribe la tabla, no mueve datos, no invalida índices ni la constraint `UNIQUE`. El riesgo real no es la base sino la desincronización con el código, por eso migración y renombre van en el **mismo commit**: aplicar una sin el otro rompe el arranque. No confundir con `usuario_creado_id`, que no se tocó. **Cambio ROMPEDOR de contrato:** quien invite usuarios mandando `supabaseUserId` ahora recibe **400**. Marcado como tal en `docs/api/CONTRATO_IDENTIDAD.md`; **pendiente avisar al frontend**. **`CLAUDE.MD` corregido en 12 lugares**: decía *"Auth se queda en Supabase"* y describía un `SupabaseJwtAuthenticationFilter` inexistente. Como `CLAUDE.MD` se carga en cada sesión y `MODULO_AUTH.md` no, la contradicción se propagaba sola hacia adelante. **Hallazgo aparte, corregido:** `docs/api/CONTRATO_IDENTIDAD.md` documentaba `POST /api/v1/account-requests` pidiendo `supabaseUserId` y omitiendo `verificationToken` y `contrasena` — un cliente que siguiera ese contrato recibía 400 en los tres campos. **Fuera de alcance, sin tocar:** `C:
enaserPlayStore\src\lib\supabase.ts` sigue existiendo en el frontend; si ahí queda auth contra Supabase, cliente y backend están desalineados. `./mvnw clean test`: **1672/1672 en verde**, con V11 aplicada por Flyway en Testcontainers y `AccountRequestPersistenceAdapterTest` haciendo ida y vuelta real contra la columna renombrada | 2026-08-31 | Este §8; `docs/MODULO_AUTH.md`; `CLAUDE.MD` §5.3.1/§5.3.5/§5.3.6/§6/§10/§11/§12; `docs/api/CONTRATO_IDENTIDAD.md`; `docs/BITACORA_ERRORES.md` E-51 |
| D-54 | **Se construyó el `S3AlmacenamientoAdapter` real: cierra el bloqueante que D-34 abrió y D-50 dejó explícito.** Hasta hoy `AlmacenamientoPort` tenía un solo adaptador, `NoOpAlmacenamientoAdapter`, que devolvía `about:blank#pendiente-s3/<ruta>` — o sea que los **9 servicios** que ya llaman a `firmarLectura` (audioterapia, portadas de cursos, portadas de eventos, media del muro, testimonios, firma de contratos, adjuntos de tickets, avatares) le entregaban al cliente móvil una URL que no apunta a nada. Los binarios estaban en S3 desde D-50; lo único que faltaba era esto. **Qué activa el adaptador real:** `renaser.storage.proveedor=s3` (variable `STORAGE_PROVEEDOR`) **y** credenciales que el SDK pueda resolver por sí mismo. Sin la propiedad, `matchIfMissing=true` deja al `NoOp` como default y nada cambia — el entorno local y los tests no necesitan cuenta de AWS. Mismo patrón exacto que `SmtpEnviarEmailAdapter`/`NoOpEnviarEmailAdapter`. **Las credenciales NUNCA van al yaml ni al código:** `DefaultCredentialsProvider` las busca en el orden estándar del SDK (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`, perfil `~/.aws/credentials`, rol IAM de la instancia) — es lo que permite usar el perfil local para probar y un rol en el despliegue sin cambiar una línea. **Permiso IAM mínimo del principal: `s3:GetObject`, `s3:PutObject` y `s3:DeleteObject` sobre `arn:aws:s3:::s3-renaser90dias/*`, y nada más** — ni `ListBucket`, ni otro bucket, ni acciones de administración. **El backend nunca toca los bytes:** subir y bajar van por URL prefirmada directamente entre el cliente y S3, así el archivo no pasa por la JVM ni ocupa un hilo mientras dura la transferencia (CLAUDE.MD §3). El único verbo que ejecuta el servidor es el borrado, con el `S3Client` síncrono. Cada caso de uso decide la validez de su URL y el adaptador la respeta tal cual: una URL prefirmada **es una credencial de acceso a ese objeto**, vive hasta que vence, así que las validezas cortas son la medida de seguridad, no una molestia. **Dependencia:** BOM `software.amazon.awssdk:bom` **2.42.41** + artefacto `s3` (el `S3Presigner` viene dentro: en el SDK v2 no es un módulo aparte como en la v1), con `netty-nio-client` **excluido** — es el cliente HTTP asíncrono, que no se usa, y arrastraba una línea de Netty distinta a la que ya gestiona Spring Boot. El cliente síncrono (`apache-client`) sigue viniendo por transitiva. **Probado sin red y sin credenciales reales** (`S3AlmacenamientoAdapterTest`): el presigner firma **localmente** con HMAC, así que una clave inventada produce igual una URL inspeccionable — se verifica que firma contra el bucket configurado, que la ruta pedida está en la URL, que `X-Amz-Expires` refleja la validez que le pasaron y que `content-type` queda entre los encabezados firmados de la subida. **Lo que esa prueba NO cubre, a propósito:** que AWS acepte la firma, que el objeto exista y que el principal tenga los permisos IAM — eso exige red y cuenta real, se verifica en el despliegue | 2026-08-31 | Este §8 (D-34, D-50); `shared/infrastructure/storage/S3AlmacenamientoAdapter.java`; `AlmacenamientoS3Config.java`; `docs/MODULO_SUPPORT.md` D-S5 |
| D-55 | **El avatar pasa a ser un objeto de lectura PÚBLICA con URL permanente — corrige D-53 de `docs/MODULO_USERS.md`, que persistía una URL prefirmada (E-57).** El defecto: `AvatarService.confirmar()` firmaba una URL de lectura con validez de **7 días** (el máximo que permite SigV4) y la guardaba como texto en `usuarios.avatar_url`. Una URL prefirmada **es una credencial con vencimiento**: a los 7 días del último cambio de foto dejaba de servir y nadie la volvía a firmar, porque el único punto que firmaba era la confirmación. No afectaba solo al perfil — el mismo string sale en el muro, comentarios, chat, miembros de célula, testimonios y panel admin, todos vía `users.api.UserSummary`. No se notaba porque el `NoOpAlmacenamientoAdapter` devuelve `about:blank`: **estaba escrito para romperse el día que se activara S3 (D-54), una semana después, y sin un solo error en el log.** **La decisión, tomada por el dueño del proyecto:** el objeto del avatar es público y la columna guarda su URL permanente — el nombre `avatar_url` ahora dice la verdad. **Por qué no "firmar al leer"**, que era la otra opción y es la correcta para el resto de los binarios: firmar en cada respuesta cambia la URL cada vez y eso **invalida el caché de imagen del cliente** — un muro con 20 avatares volvería a descargar las 20 fotos en cada pantallazo. El avatar es el activo de menor sensibilidad y el que más se repite por respuesta; es el patrón de GitHub/Slack, y el dueño aceptó explícitamente que la ruta sea adivinable. **Alcance del cambio:** `AlmacenamientoPort` gana `urlPublica(ruta)` (implementado con `S3Utilities` en el adaptador real, sin llamar a AWS; con el mismo marcador en el `NoOp`); `AvatarService.confirmar()` guarda esa URL y desaparecen `VALIDEZ_URL_LECTURA` y los 7 días; **la subida NO cambia** — sigue prefirmada a 10 minutos, escribir en el bucket nunca es público. **Que no vuelva a pasar, en dos capas:** `User.changeAvatar` rechaza cualquier valor con marcas de SigV4 (`X-Amz-Signature`/`X-Amz-Credential`/`X-Amz-Expires`), y `V13` agrega el `CHECK` equivalente en `usuarios` **y** en `testimonios`. **Migración `V13__avatar_url_permanente.sql`:** sin cambios de estructura; corta la query string de las filas prefirmadas (`split_part(avatar_url, '?', 1)` — exacto, porque en SigV4 todo lo que caduca va después del `?`) y pone `NULL` en las que quedaron con el marcador `about:blank` del `NoOp` (no son reparables y no tienen objeto detrás: con el `NoOp` activo la URL de **subida** también era un marcador, así que nunca se subió nada). Las `NULL` previas no se tocan. Mismo tratamiento para `testimonios.avatar_url`, que copia el avatar del autor al promover y heredaba el vencimiento. **`chat` y `community` no necesitaron cambios**: reciben la URL lista dentro de `UserSummary`. **⚠️ REQUISITO DE INFRAESTRUCTURA, no vive en el código:** el bucket debe permitir **`s3:GetObject` anónimo sobre el prefijo `avatares/*`** — hay que desactivar *Block Public Access* para el bucket y agregar la bucket policy correspondiente. S3 bloquea el acceso público por defecto, así que **sin ese cambio la URL es correcta y devuelve 403**. Los permisos IAM del **backend** no cambian: siguen siendo los mínimos de D-54 (`GetObject`/`PutObject`/`DeleteObject` sobre `arn:aws:s3:::s3-renaser90dias/*`). Todo lo que no sea `avatares/` sigue privado y se sirve con URL prefirmada corta | 2026-08-31 | Este §8 (D-34, D-50, D-54); `docs/MODULO_USERS.md` §10; `docs/BITACORA_ERRORES.md` E-57; `shared/application/ports/out/AlmacenamientoPort.java`; `V13__avatar_url_permanente.sql` |
| D-56 | **Supabase queda descartado tambien como base de datos del producto final — decision explicita del dueno del proyecto (2026-08-31).** D-53 habia sacado el ultimo rastro de Supabase **Auth**; lo que quedaba abierto era **donde vive la base**: `CLAUDE.MD` §11 listaba "Instancia propia | Supabase Postgres" como **Sin decidir**. Ya no: la base del producto final es propia y la define nuestro esquema Flyway (schema `renaser`, 92 tablas, V1..V13), no un proyecto de Supabase. **Verificado antes de escribirlo, no asumido:** `grep -ri supabase` sobre `src/`, `pom.xml` y `application.yaml` no devuelve ni una dependencia, propiedad ni ruta de codigo — las 20 apariciones que quedan son **javadoc que describe el sistema viejo** ("reemplaza la RPC de Supabase", "la app hoy escribe directo a Supabase"), contexto historico util que se deja a proposito. De Supabase solo quedan **datos de origen ya extraidos** (D-50: binarios migrados a S3; D-51/D-52: catalogos cargados por Flyway). **Lo unico que sigue abierto es donde se hostea esa instancia propia** (RDS / Cloud SQL / VPS): es infraestructura, no arquitectura, y no bloquea nada — hoy corre en Docker local (`pgvector/pgvector:pg16`, puerto 5433) y no hay entorno desplegado. **`CLAUDE.MD` corregido en 4 lugares:** §2 (la tabla de diagnostico ahora dice explicitamente que describe el **sistema de origen**, no el que se esta construyendo — decia "| Capa | Hoy |", que invitaba a leer "Supabase Auth" como estado actual), §6 fila Persistencia, §11 fila Base de datos (pasa de *Sin decidir* a **A resuelto**) y §12 (el bloqueante *"Auditar la RLS de `INSERT` en `public.users`"* se cierra **por irrelevancia**: no hay RLS que auditar; lo que queda es verificar el `GRANT` sobre `renaser.usuarios` del carril de escritura, §5.3.6). **Fuera de alcance, sin tocar:** `C:
enaserPlayStore\src\lib\supabase.ts` sigue existiendo en el frontend — mismo pendiente que ya dejo anotado D-53 | 2026-08-31 | Este §8 (D-50, D-53); `CLAUDE.MD` §2/§6/§11/§12 |
| D-57 | **Se cierran los dos hallazgos de programacion funcional (§5.4.7) que dejo abierta la auditoria del 2026-08-31 — los unicos dos en 1.551 archivos.** (1) **`Optional` como componente de `record`**, en `ConsultarAudioterapiaSemanalUseCase.EstadoAudioterapia`: §5.4.7 lo prohibe textualmente (*"`Optional` como campo de clase o parametro de metodo"*) y un componente de `record` es un campo. Se resolvio con **`sealed interface` + pattern matching**, que es lo que la misma seccion recomienda para "resultados con variantes cerradas". **El motivo real no era cosmetico:** los dos `Optional` nunca eran independientes — el servicio devolvia **siempre ambos vacios o ambos presentes**, asi que eran dos variantes cerradas disfrazadas de dos campos opcionales. Ahora son `AudioDeLaSemana(int semanaActual, String titulo, String url, Integer diaSiguienteCambio)` y `EsperandoContenido()`: el estado ilegal ("semana sin audio") pasa a ser **irrepresentable**, y el `switch` de `AudioTherapyStatusResponse.from` es exhaustivo por compilador. Sigue el precedente que ya existia en el repo (`ConsultarClaseDiariaUseCase.ClaseDiariaResolution`), no introduce un estilo nuevo. **El contrato HTTP no cambia, verificado leyendo el camino completo de serializacion (§8, §10):** `AudioTherapyStatusResponse` conserva identica su firma `(Integer semana, String titulo, String url, Integer diaSiguienteCambio)`; lo unico que cambio es como se calculan los argumentos. El tipo del puerto nunca se serializo a HTTP — grep de `EstadoAudioterapia`/`AudioResuelto` sobre `src/` no lo encuentra fuera del puerto, el servicio, el DTO y el test. (2) **Cadena de stream de 5 pasos** en `MiembroService.listar(actorId, query, cursor, limite)`: §5.4.7 pide extraer a metodo privado con nombre por encima de ~3 pasos. Se extrajo a **`destinatariosPosiblesDeUnDmNuevo(global, actorId)`** — nombre de intencion de negocio ("a quien se le puede escribir"), no de mecanica, segun §5.4.8 (*"el metodo privado existe para ponerle nombre a una idea, no para acortar lineas"*). Los dos comentarios que explican el porque de cada filtro viajaron con su paso, textuales. **Lo que deliberadamente NO se toco:** las dos sobrecargas de `listar` siguen separadas — la del roster a proposito **no** filtra por `UserStatus`, y unificarlas habria sido un cambio de comportamiento disfrazado de refactor. **Sobre el informe de auditoria:** `docs/informes/auditoria-arquitectura-codigo.md` **no se edito a proposito** — es un registro fechado de lo que se encontro ese dia, no una guia viva; marcar ahi el hallazgo como resuelto falsificaria el registro. El estado vivo es esta entrada. **Sigue abierto, fuera del alcance de esta pasada:** el eje 2 de esa misma auditoria (controllers que orquestan dos casos de uso, 13 de 263 handlers, severidad alta). `./mvnw clean test`: **1886/1886 en verde** | 2026-08-31 | Este §8; `docs/informes/auditoria-arquitectura-codigo.md` §5; `CLAUDE.MD` §5.4.7/§5.4.8 |
| D-58 | **Se acota §5.4.7: la inmutabilidad estricta aplica a value objects y comandos, no a las raices de agregado — decision del dueno del proyecto (2026-08-31), tomada sobre la recomendacion.** El barrido nuevo de §5.4.7 (agente auditor, 1554 archivos) devolvio como hallazgo de severidad **alta** 216 campos no-`final` con mutadores `void` en **36 raices de agregado**. **No era un defecto de codigo: era una contradiccion del propio `CLAUDE.MD`.** §5.4.7 pedia *"campos `final`, los metodos que 'cambian' devuelven una instancia nueva"* para todo el dominio, mientras §5.3.2 prescribe el patron opuesto **como ejemplo normativo** (`public void suspend() { this.status = SUSPENDED; }`, sobre campos no-`final`). Con las dos vivas, el punto era **inauditable**: contra §5.4.7 se marcan 216 campos, contra §5.3.2 no se marca ninguno. **Se resolvio por documentacion, sin tocar una linea de codigo.** La regla vigente: inmutabilidad estricta para **value objects, comandos y DTOs**; las **raices de agregado mutan solo por metodos con nombre de intencion** (`suspend()`, `asignarMentor()`), nunca por setters. No es una regla nueva — es lo que el documento ya decia en §5.3.2 y en §5.4.5 (*"prohibido cualquier cosa que abra una forma de mutar el estado **sin pasar por un metodo con nombre de intencion**"*) y lo que ya estaba registrado en `docs/MODULO_POINTS.md` D-P2. **Por que NO se reescribieron los 36 agregados:** habria contradicho §5.3.2 y D-P2, tocado 216 campos mas sus servicios y tests, y es exactamente la clase de reescritura que §0.4 manda evitar (*"no romper lo que ya funciona"*). El codigo no estaba mal; el documento se pedia dos cosas opuestas. **Lo que sigue prohibido, sin cambios:** `@Data`, `@Setter`, `@NoArgsConstructor` publico y cualquier `public void setX(...)` en `domain/` — hoy hay **0** de los tres en los 214 archivos de dominio, y esa es la parte de la regla que de verdad protege los invariantes. Un agregado inmutable de punta a punta (`Conversacion.renombrada(...)`, `ConversacionRenasia`) sigue siendo valido y preferible cuando es chico; lo que deja de ser obligatorio es forzarlo en los 36. **Queda abierto, con decision pendiente:** los 33 `UUID.randomUUID()` en `domain/`, que violan literalmente el *"sin aleatoriedad"* de la misma seccion (el reloj si esta resuelto: 0 usos de `Instant.now()` en los 214 archivos, todo por el puerto `Clock`) | 2026-08-31 | Este §8 (D-P2 de `docs/MODULO_POINTS.md`); `CLAUDE.MD` §5.4.7/§5.3.2/§5.4.5 |
| D-59 | **La identidad sale de `domain/` y entra por un puerto `IdGenerator`, hermano de `Clock` — decision del dueno del proyecto (2026-08-31) tomada sobre evidencia de las fuentes de referencia, no sobre una opinion.** El barrido de §5.4.7 encontro **32 archivos de `domain/` llamando a `UUID.randomUUID()`**, contra 0 usos de `Instant.now()`: el reloj estaba resuelto con un puerto y la identidad no. **Que se investigo antes de decidir** (a pedido explicito del dueno, leyendo el codigo de los repos, no articulos sobre ellos): `citerus/dddsample-core` declara `CargoRepository.nextTrackingId()` en el dominio y lo resuelve en infraestructura; Vaughn Vernon hace lo mismo con `ProductRepository.nextIdentity()`; `thombergs/buckpal` va mas lejos y no genera identidad en el dominio en absoluto (`Account.withoutId(...)` nace con `id = null` y la asigna la base); `kgrzybek/modular-monolith-with-ddd` si usa `Guid.NewGuid()` en el dominio, pero es inconsistente consigo mismo (`Member.Create(Guid id, ...)` lo recibe por parametro) y es igual de laxo con el reloj. **Cockburn no dice nada del tema — ausencia verificada**: hexagonal es una regla sobre direccion de dependencias. Se eligio la postura del puerto; se descarto la de `buckpal` porque con Modulith Events un agregado sin id hasta el `flush` complica publicar el evento de creacion con su propio identificador. **El argumento decisivo no fue de autoridad sino de consistencia interna:** ya se habia pagado el costo de esta regla con `Clock` y se estaba cobrando solo la mitad del beneficio. **El patron:** el value object de identidad se queda en `domain/` con `of(UUID)` y pierde `newId()`; la factoria del agregado recibe el id; el caso de uso inyecta el puerto. El dominio **no importa** `IdGenerator`. **El beneficio concreto, no teorico:** dos tests estaban construyendo su fixture con `rehydrate()` — el camino del adaptador, que **se saltea todas las validaciones del agregado** — unicamente porque `crear()` sorteaba un id y rompia el mock (`RecordatorioServiceTest` de `calendar`, `CohorteServiceTest` de `community`). Los dos volvieron a la factoria real. **Casos donde el patron NO se aplico, con criterio:** `ConversacionRenasia` no tiene id propio (su identidad **es** el `UserId`, PK = FK); `RocaMaestra` no tiene factoria de creacion (RK-1); los UUID que arman rutas de storage o ids de filas hijas en adaptadores no son identidad de dominio. **El caso limite fue `MediaOnboarding.rutaNueva()`**: no es identidad (el id de `medias_onboarding` es un `Long` de Postgres) pero vive en `domain/`. Se paso el UUID por parametro **sin** mover la construccion de la ruta, porque su gemelo `registrar()` valida `rutaStorage.startsWith(prefijoDe(usuarioId))` — separar constructor y validador dejaria las dos mitades de un invariante de seguridad en capas distintas. **Lo que impide que el terreno se recupere:** una regla ArchUnit que prohibe `randomUUID()` en `..domain..`, con una lista de excepciones que bajo de 12 a 0 modulo por modulo y ya no existe. **Como se hizo:** 12 commits, uno por modulo, cada uno revertible solo. **Queda abierto, reportado y NO tocado a proposito:** ~25 factorias cruzan el techo de 4 parametros de §5.4.8 — `Evento.crear` (21), `RocaDiaria.planificar` (13), `Evidencia.registrar` (13), `Mensaje.escribir` (12), `Testimonio.crear` (10)… **La gran mayoria ya lo violaba antes de esta migracion**; solo 6 lo cruzan por el `+1` del id. Convertirlas a `record` de comando es trabajo aparte y su propia decision. `./mvnw clean test`: **1887/1887 en verde** | 2026-08-31 | Este §8 (D-58); `CLAUDE.MD` §5.4.7; `shared/domain/IdGenerator.java`; `ArchitectureTest.domainDoesNotGenerateItsOwnIdentity` |
| D-60 | **Vincular una identidad social a una cuenta existente se hace con un vinculo EXPLICITO, no auto-vinculando por correo verificado — decision del dueno del proyecto (2026-09-01).** El hueco era real: cuando el correo ya tenia cuenta pero la identidad social no estaba vinculada, `POST /auth/social` devolvia 409 diciendo "entra con tu contrasena" y despues **no existia ninguna forma de conectar Google**. La alternativa evaluada era vincular automaticamente si el correo del proveedor viene verificado y coincide con el de la cuenta; se descarto porque el costo del camino explicito es **una pantalla** y el riesgo que cubre es **apropiacion de cuenta** — es lo que recomiendan Auth0 y Clerk: autenticar **las dos** identidades antes de unirlas (la nuestra con la sesion, la del proveedor con el `code`). Se agrego `POST /api/v1/auth/social/link` (204 / 409 si la identidad ya es de otro usuario / 401 sin sesion), que **exige sesion real y NO acepta el respaldo de `X-Actor-Id`** — es el segundo endpoint del sistema con esa exigencia, junto a `GET /auth/me`: vincular con un header que cualquiera escribe seria el mismo agujero que el endpoint viene a cerrar. **No** se exige que el correo del proveedor coincida con el de la cuenta (vincular un Google personal a una cuenta de trabajo es legitimo; lo que impide que una cuenta del proveedor sirva a dos usuarios es la `UNIQUE (proveedor, sujeto_proveedor)`, no una comparacion de correos). **Pendiente declarado:** desvincular, que depende de una regla de producto sin confirmar (si el usuario entro por Google y no tiene contrasena, desvincular lo dejaria sin forma de entrar) | 2026-09-01 | `docs/MODULO_AUTH.md` §6.9 |
| D-61 | **El telefono deja de ser obligatorio en el alta y se pide en la Ficha Inicial del onboarding — decision del dueno del proyecto (2026-09-01).** El motivo es de producto: el registro tiene que ser lo mas liviano posible (correo, nombre y contrasena) porque es el unico momento del flujo donde la persona todavia no invirtio nada y abandona mas facil; los datos completos, telefono incluido, se recogen despues, en la Ficha Inicial, cuando ya hay compromiso. **El sintoma que cerro:** el frontend ya habia dejado de mandarlo, asi que `POST /api/v1/account-requests` llegaba con `phone: null` y respondia **400** — nadie podia registrarse. **Estaba exigido en cinco lugares y habia que bajarlo en los cinco, o no cambiaba nada:** (1) `solicitudes_cuenta.telefono NOT NULL` en Postgres desde el baseline V1 → migracion **V14** con `DROP NOT NULL`; (2) `@NotBlank` en `SubmitAccountRequestRequest`; (3) `@NotBlank` en `SubmitAccountRequestCommand`; (4) el invariante `requireNotBlank(phone, ...)` del agregado `AccountRequest`, que ahora **normaliza a NULL** un telefono en blanco ("no lo dio" tiene que ser un solo valor en la columna, no null y `""` a la vez); (5) `AutenticacionSocialService.requirePhoneParaAlta`. **Efecto colateral buscado, no accidental: se desbloqueo el alta por Google.** El punto (5) rechazaba con 400 toda cuenta nueva por login social — Google/Apple/Facebook no devuelven telefono y el boton social no lo mandaba —, con el agravante de que el `code` de OAuth es de un solo uso y el intento fallido lo consumia igual, obligando a reiniciar el flujo del navegador para reintentar. Eso cierra **A-8** de `docs/MODULO_AUTH.md`, que estaba abierto esperando exactamente esta decision de producto. **Lo que NO cambia:** el telefono se sigue guardando si viene (no se borro ni la columna ni el campo), `usuarios.telefono` ya era nullable y no se toco, la contrasena sigue con minimo 12, y `fullName` sigue siendo obligatorio y un solo campo (el frontend concatena nombre y apellidos; el backend no se entera y asi tiene que quedar). **Pruebas:** se **invirtio** `AutenticacionSocialServiceTest.identidadNuevaSinTelefonoRechazadaSinLlegarASubmit` — que afirmaba lo contrario — a `identidadNuevaSinTelefonoSeDaDeAltaIgualConTelefonoNull`, y se agregaron el caso de dominio (telefono null y en blanco), el del comando (por formulario y por proveedor social), el INSERT con `telefono` NULL contra Postgres real, y el alta por Google sin telefono de punta a punta con aprobacion incluida | 2026-09-01 | `docs/MODULO_AUTH.md` §6.7/§10 (A-8); migracion `V14__solicitudes_cuenta_telefono_opcional.sql`; `docs/api/CONTRATO_IDENTIDAD.md` |
| D-62 | **Las fotos y los videos del Muro se guardan en prefijos separados de S3 (`muro/fotos/` y `muro/videos/`) — decision del dueno del proyecto (2026-09-01).** Antes, `PublicacionMuroService.solicitarUrl` armaba `muro/{actorId}/{uuid}` para cualquier archivo. **El motivo no es orden visual:** en S3 el prefijo es lo unico sobre lo que se pueden aplicar reglas distintas por tipo de archivo — politica de ciclo de vida, clase de almacenamiento, o un permiso de lectura que valga para las fotos y no para los videos. Con todo mezclado en una carpeta, cualquiera de esas decisiones obliga a inspeccionar objeto por objeto. **El tipo se valida ahora en `solicitarUrl`, no solo en `MediaPublicacion`:** la URL se firma **antes** de que exista la publicacion, asi que sin ese corte se firmaba una subida para un archivo que el dominio iba a rechazar despues, dejando un objeto huerfano en el bucket. Un `tipoContenido` que no empiece con `image/` ni `video/` se rechaza sin firmar nada. **Infraestructura ya creada:** los prefijos `muro/fotos/` y `muro/videos/` existen en el bucket `s3-renaser90dias` (cuenta 526338061654, `us-east-1`), creados con el perfil `renaser` del AWS CLI. **No hubo que migrar nada**: el almacenamiento estuvo en `NoOp` hasta hoy, asi que no habia ni un objeto bajo `muro/`. **Credenciales:** no se declaran en ninguna variable — `AlmacenamientoS3Config` usa `DefaultCredentialsProvider`, que resuelve el perfil de `~/.aws/credentials`; alcanza con `STORAGE_PROVEEDOR=s3` y `AWS_PROFILE=renaser`, y la clave secreta no se duplica fuera del archivo de AWS. **Pruebas:** 3 nuevas en `PublicacionMuroServiceTest` (imagen al prefijo de fotos, video al de videos, y un `application/pdf` rechazado **antes** de firmar); no se afirma la ruta completa porque el UUID final es aleatorio y afirmarlo entero volveria la prueba fragil sin verificar nada mas. `./mvnw test -Dtest=PublicacionMuroServiceTest`: **28/28 en verde** | 2026-09-01 | `docs/ENDPOINTS_FALTANTES.md` (C-1); `PublicacionMuroService.rutaDeMedia` |
| D-63 | **Ranking: caché Caffeine con TTL de 20s en `RankingPersistenceAdapter`, no procedimiento almacenado ni vista materializada ni tabla desnormalizada por eventos — decision del dueno del proyecto (2026-09-01).** Se evaluaron las cuatro opciones. Se descartaron las otras tres por la misma razon: **hoy el calculo es trivial** (18 aprendices, 2 puntajes en la base) — el problema de saturacion que motivo la pregunta **todavia no existe**, y construir maquinaria pesada para un problema inexistente es exactamente el tipo de sobre-ingenieria que `CLAUDE.MD` §3 pide evitar. La caché **no compromete ninguna decision futura**: si el calculo llega a pesar de verdad, la tabla desnormalizada por eventos se agrega detras del mismo puerto (`LoadRankingPort`), sin tocar dominio ni el contrato con la app movil. **Que se cacheo:** solo `porTipoYFecha(tipo, fecha)` — la lectura que sirve `GET /api/v1/ranking` y `GET /api/v1/ranking/{tipo}` y la que de verdad tiene fan-out alto (muchas personas mirando el mismo podio). `aprendicesActivosConPuntaje()` (usado por el snapshot nocturno, 1 vez/dia) no se cacheo — no tiene el problema de lecturas concurrentes. **Clave de cache: `tipo + fecha`, sin el actor.** Se verifico leyendo `RankingService.consultar` que el actor solo se usa para `requireActorActivo` (autorizacion: exige cuenta ACTIVE) y nunca se pasa a `LoadRankingPort.porTipoYFecha` — la consulta a la base no varia segun quien pregunta, asi que meter al actor en la clave solo fragmentaria el cache sin necesidad (y omitirlo cuando SI varia por actor hubiera sido el bug clasico de filtrar el ranking de una persona a otra). **TTL: 20s**, dentro del rango 15-30s que el dueno del proyecto aprobo como imperceptible para quien mira el podio. Se eligio el extremo bajo del rango porque el ranking no tiene todavia canal de invalidacion entre instancias (`CLAUDE.MD` §5.2.1 — Caffeine es por instancia; Redis Pub/Sub para esto no existe aun), asi que el TTL es hoy la unica red de seguridad real ante staleness entre replicas. **Invalidacion activa:** `reemplazar()` (llamado por `SnapshotRankingScheduler`, 1 vez/dia a las 05:05 UTC) hace `@CacheEvict` de la misma clave `tipo+fecha` — el snapshot nuevo no espera el TTL para reflejarse en la instancia que lo genero. **Donde vive:** el `CacheManager` (motor, politica de TTL/tamaño) esta en `shared/infrastructure/cache/CacheConfig.java` porque es infraestructura transversal, no logica de negocio — cualquier modulo puede declarar su propio `@Cacheable` reusando este mismo motor. Las anotaciones `@Cacheable`/`@CacheEvict` en si viven en `RankingPersistenceAdapter` (adaptador de persistencia), nunca en `domain/` ni en `application/`. **Señal para escalar a la tabla por eventos:** cuando el dashboard de Micrometer/OTel (§12, todavia pendiente) muestre la consulta de ranking apareciendo en el p99 con carga real — no antes, y no por intuicion | 2026-09-01 | `CLAUDE.MD` §3, §6; `RankingPersistenceAdapter`; `RankingPersistenceAdapterCacheTest`; `docs/ENDPOINTS_FALTANTES.md` (R-1) |
| D-65 | **El registro por Google/Apple/Facebook pasa a dos pasos — identidad pendiente en Redis + formulario de confirmacion — decision del dueno del proyecto (2026-09-01).** Hasta ahora `POST /api/v1/auth/social` creaba la `AccountRequest` en la MISMA llamada que verificaba la identidad: la app nunca llegaba a mostrarle a la persona un formulario con su correo/nombre ya prellenados, como manejan las redes sociales. **El obstaculo tecnico que fuerza el diseño en dos pasos:** el `code` de OAuth es de un solo uso, y la app no conoce el correo/nombre hasta que el backend lo canjea contra el proveedor — asi que no hay forma de prellenar un formulario sin retener la identidad ya verificada en algun lado ANTES de gastar el `code`. **Que se construyo:** `TokenRegistroPendienteSocialPort` + `TokenRegistroPendienteSocialRedisAdapter` (mismo patron que `TokenResetContrasenaPort`/`CodigoVerificacionEmailPort`: token opaco de 256 bits, `GETDEL` atomico para "un solo uso" real, TTL nativo de Redis de **10 minutos — igual al OTP de alta** — sin cron de purga); el record `RegistroPendienteSocial` (proveedor, sujeto, email, fullName), serializado con un separador de control U+0001 en vez de sumar Jackson por un solo caso. `AutenticacionSocialService.iniciarSesion` para identidad nueva ya NO llama a `SubmitAccountRequestUseCase`: genera el token y devuelve la variante nueva `ResultadoLoginSocial.RegistroPendiente(token, email, fullName)` — la variante vieja `SolicitudCreada` desaparecio del sellado. `POST /api/v1/auth/social/complete` (nuevo, `CompletarRegistroSocialUseCase`/`CompletarRegistroSocialService`) recibe `{registroPendienteToken, fullName, phone?, city?}`, consume el token y recien ahi arma el `SubmitAccountRequestCommand.porProveedorSocial` — mismo camino que ya usaba el paso unico anterior (genera el `verificationToken` directo, sin pedir el codigo de 6 digitos, porque el proveedor ya confirmo el correo). **La regla de seguridad innegociable:** el correo y el `sujeto` del proveedor salen SIEMPRE del registro que devuelve `consumir()`, NUNCA del cuerpo de `/complete` — el DTO ni siquiera tiene un campo para mandarlos, mismo blindaje por compilador que el `role` ausente del alta publica (CLAUDE.MD §5.3.3). Si el correo viajara por HTTP en este paso, cualquiera completaria un registro con el correo de otra persona: exactamente el agujero de apropiacion de cuenta que D-60 y `docs/MODULO_AUTH.md` §6.4 vienen evitando. `fullName` SI viaja del cliente (la persona puede corregir como se escribe su nombre); `phone`/`city` siguen opcionales (D-61). **Contrato que cambia:** `SolicitudSocialResponse.EstadoSolicitudSocial` pierde la variante `CREADA` (ya no se produce desde `/social`); queda solo `EN_REVISION`. La identidad nueva ahora responde con un cuerpo distinto, `RegistroPendienteSocialResponse{registroPendienteToken, email, fullName}`, tambien 202. Las otras tres salidas de `/auth/social` (200 sesion, 202 `EN_REVISION`, 409 correo ya registrado) no cambian. **Pruebas:** se **reescribio** `LoginSocialCicloCompletoIntegrationTest` al ciclo nuevo (primer toque → `RegistroPendiente` → `completar()` → `AccountRequest` creada → segundo toque mientras pendiente → `SolicitudEnRevision` → aprobacion → tercer toque → sesion), incluida la variante sin telefono de D-61; se reescribio `AutenticacionSocialServiceTest` (ya no depende de `SubmitAccountRequestUseCase`/`TokenVerificacionEmailPort`, ahora de `TokenRegistroPendienteSocialPort`); se agrego `CompletarRegistroSocialServiceTest` (la prueba central: mandar un correo distinto en el cuerpo no tiene ningun efecto, porque no hay campo para mandarlo) y `TokenRegistroPendienteSocialRedisAdapterTest` (Testcontainers: GETDEL atomico, TTL real, ida y vuelta con nombres con tildes/apostrofes) y `CompletarRegistroSocialControllerTest`/`LoginSocialControllerTest` actualizado. `./mvnw test`: ver resultado completo en la bitacora de esta tarea | 2026-09-01 | `docs/MODULO_AUTH.md` §6.10; `docs/api/CONTRATO_IDENTIDAD.md`; `docs/ENDPOINTS_FALTANTES.md`; `TokenRegistroPendienteSocialPort.java`; `AutenticacionSocialService.java`; `CompletarRegistroSocialService.java`; `AutenticacionController.java` |
| D-64 | **A-1 se cierra parcialmente: `@RequiresPermission` ya se EJECUTA, pero solo para TRAINEE — decision del dueno del proyecto (2026-09-01).** El hueco era el mas grande abierto (`docs/ENDPOINTS_FALTANTES.md` A-1): los 219 endpoints declaraban que permiso exigian, pero ningun filtro ni interceptor lo hacia cumplir — cualquiera con cualquier rol podia llamar cualquier endpoint. **Se construyo `UserRole.can(Permission)`** (`users/api/UserRole.java`, matriz en un solo archivo, CLAUDE.MD §5.3.2) y **`PermissionEnforcementInterceptor`** (`users/infrastructure/adapter/in/web/security`, registrado via un `WebMvcConfigurer` propio de `users` para no crear una dependencia ciclica `shared`→`users`→`shared`). **Alcance deliberado y ACOTADO por el dueno del proyecto: solo TRAINEE tiene matriz real hoy.** Los 8 permisos con evidencia en el codigo (guards ya existentes en cada servicio): `USE_APP`, `FOLLOW_OWN_PROGRAM`, `PUBLISH_ON_WALL`, `OPEN_SUPPORT_TICKET`, `USE_MENTOR_TICKETS`, `OPEN_MENTOR_TICKET`, `VIEW_OWN_PHASE_CONTRACTS`, `SIGN_PHASE_CONTRACT`. Los otros 22 (los 7 `MANAGE_*`, `MODERATE_WALL`, `ADJUST_POINTS`, `APPROVE_ACCOUNT_REQUEST`, `ASSIGN_MENTOR`, `MANAGE_ROLES`, `ANSWER_MENTOR_TICKET`, `VIEW_ALL_MENTOR_TICKETS`, `TRACK_PROGRAM_AS_STAFF`, `PROMOTE_TESTIMONIAL`, `RENAME_GLOBAL_CHAT`, `VIEW_ONBOARDING_DASHBOARD`, `MANAGE_EVIDENCE`) quedan fuera de TRAINEE. **MENTOR, MENTOR_LEAD, ADMIN y ALCHEMIST no tienen matriz: `can()` devuelve `true` para cualquier permiso (falla-abierto, no falla-cerrado), a proposito — CLAUDE.MD §0.6 prohibe inventar que puede hacer cada uno de esos 4 roles sin que el dueno lo confirme.** El interceptor tampoco aplica el chequeo de cuenta `SUSPENDED` a esos 4 roles (pasan exactamente igual que antes de este cambio); para TRAINEE, `SUSPENDED` corta con 403 salvo `OPEN_SUPPORT_TICKET` (`Permission.toleraCuentaSuspendida()`), porque alguien suspendido tiene que poder reclamar su suspension. **Login/registro no se tocan:** los endpoints `@PublicEndpoint` nunca pasan por la verificacion, y el respaldo `X-Actor-Id` (fase 4 todavia no llego) sigue funcionando igual — el interceptor lee el mismo header con la misma precedencia que `ActorAutenticadoArgumentResolver` (sesion primero), pero implementado aparte para no arriesgar el comportamiento ya probado de esa clase. **Riesgo de infraestructura evitado a proposito:** el interceptor recibe `UserSummaryFinder` envuelto en `ObjectProvider<>` en vez de como dependencia obligatoria — Spring Boot incluye automaticamente cualquier `WebMvcConfigurer`/`HandlerInterceptor` en TODOS los `@WebMvcTest` del repo, y un `@WebMvcTest` de OTRO modulo no carga el servicio real de `users`; con una dependencia obligatoria, este cambio hubiera roto todos esos tests con `NoSuchBeanDefinitionException` al arrancar el contexto. Con `ObjectProvider`, la ausencia del colaborador en esos contextos reducidos hace que el interceptor deje pasar la request tal cual pasaba antes del cambio; el contexto de produccion siempre lo tiene. Por la misma razon el cuerpo del 403 se serializa a mano (dos campos fijos) en vez de pedir un `ObjectMapper` por inyeccion, que tampoco esta garantizado en esos contextos reducidos. **Costo de latencia (CLAUDE.MD §3/§5.3.5):** el interceptor agrega, para un TRAINEE, la misma consulta indexada por PK que ya hacia `RequireActiveUserGuard` en la capa de servicio (`UserSummaryFinder.findById` → `LoadUserPort.byId`) — no es una consulta nueva, es la misma movida mas temprano. **No existe todavia una cache Caffeine de rol/estado** (la que describe CLAUDE.MD §5.3.5 con TTL de 30s): se evaluo reusar el mecanismo de `CacheConfig`/D-63, pero no existe ningun consumidor de esa cache para `UserSummaryFinder.findById` hoy — se deja explicitamente **sin construir**, como pendiente declarado, en vez de agregarla sin que el dueno del proyecto la pida (la instruccion de esta tarea fue "si no existe, decilo y proponé, no la construyas por tu cuenta"). **Pruebas:** `UserRoleTest` (37, matriz completa permiso por permiso + los 4 roles temporales), `PermissionTest` (30, `toleraCuentaSuspendida()`), `PermissionEnforcementInterceptorTest` (13, unitario: TRAINEE bloqueado/permitido, SUSPENDED, `@PublicEndpoint`, handler sin anotar, actor no resoluble, actor inexistente, ADMIN fail-open, sin `UserSummaryFinder` disponible, sesion vs header, token anonimo) y `WallControllerAuthorizationTest` (4, integracion real con `@WebMvcTest` + `MockMvc` contra `WallController`: confirma que Spring MVC de verdad ejecuta el interceptor, no solo que la clase compila). `./mvnw test` completo: ver detalle de regresiones abajo | 2026-09-01 | `docs/ENDPOINTS_FALTANTES.md` (A-1); `docs/MODULO_AUTH.md` §8.1, §9 (fase 4); `UserRole.java`; `Permission.java`; `PermissionEnforcementInterceptor.java`; `PermissionEnforcementWebConfig.java` |
| D-66 | **C-1 (`docs/informes/auditoria-seguridad-concurrencia-2026-09-01.html`, crítico) cerrado en `onboarding`/`rag`/`academy`: la IA deja de correr dentro de `@Transactional`.** `ProcesarValidacionV90Service.procesar`, `EspejoSombraService.generar`, `ConocimientoService.indexar` y `RecomendacionService.recomendacion` envolvían lectura + llamada a la IA (hasta 45s con un proveedor real) + guardado en una sola transacción — cada validación retenía una conexión de Hikari todo ese tiempo, y con varios aprendices a la vez agotaba el pool para TODA la API, no solo para el módulo que llamó a la IA. Se sacó `@Transactional` de los cuatro métodos: la lectura y la escritura ya corren en su propia transacción corta porque **Spring Data JPA la da gratis por método de repositorio** (`SimpleJpaRepository`) — no hizo falta declarar ninguna transacción nueva, alcanzó con dejar de envolver de más. **Números fijados en `application.yaml`, con su porqué:** `spring.task.execution.simple.concurrency-limit=20` (el executor de `@Async` que usa la validación V90 no tenía tope propio con hilos virtuales — 20 duplica el escenario "diez aprendices a la vez" de la auditoría y protege el rate limit del proveedor de IA, no ya el pool, que dejó de necesitarlo); `spring.datasource.hikari.maximum-pool-size=20`/`minimum-idle=5`/`connection-timeout=5000` (antes sin fijar, corría con el default implícito de 10; con transacciones cortas 20 da margen sin sobre-provisionar el Postgres único de Docker local, y 5s de timeout falla rápido en vez de amontonar requests). **Riesgo adicional cerrado en el camino:** en onboarding, si la IA real lanza (timeout, red — el `NoOp` de hoy nunca lo hace), la grabación V90 ya estaba marcada `PROCESANDO` (persistido antes del despacho `@Async`) y quedaba así para siempre porque nadie capturaba la excepción; se agregó un `try/catch` que la trata como `NO_DISPONIBLE`, reusando la máquina de estados existente (reintento o `REVISION_MANUAL`) — los otros tres servicios no tienen un estado intermedio persistido que pueda quedar atrapado, así que no necesitaron el mismo tratamiento. **Fuera de esta pasada, explícitamente:** el resto de C-3 del mismo informe (lock pesimista contra double-dispatch, barrido de `PROCESANDO` huérfanos tras un reinicio, `shutdown.await-termination`), C-4 (`EvidenciaService.procesarLote`, mismo bug de fondo, fuera de la lista de archivos del encargo) y `PgVectorNativoAdapter.buscarSimilares` (otra instancia de IA dentro de `@Transactional`, detectada de paso en `rag` pero no incluida en el alcance) | 2026-09-01 | `docs/BITACORA_ERRORES.md` E-67; `docs/informes/auditoria-seguridad-concurrencia-2026-09-01.html` (C-1); `ProcesarValidacionV90Service.java`; `EspejoSombraService.java`; `ConocimientoService.java`; `RecomendacionService.java`; `application.yaml` |
| D-67 | **El reloj del programa de 90 dias arranca a existir: activacion self-service + cron nocturno de avance + recalculo de fase — decision del dueno del proyecto (2026-09-01).** Hasta ahora `ParticipacionPrograma.avanzarDia()` existia en el dominio y no lo llamaba nadie; `dia_programa` se quedaba donde quedaba, y no habia forma de que un aprendiz activara su programa. Cierra el bloqueante que `docs/MODULO_PHASECONTRACTS.md` §0.2 señalaba explicitamente ("nada en el baseline garantiza que un cron la recalcule... ese cron todavia no existe") y explica un bug real ya presente en los datos: una fila en dia 17 con `FASE_1_RENACER` y otra en dia 17 con `FASE_2_DESARROLLO`. **La regla de negocio, con una correccion durante la tarea:** tras Terminos y Condiciones el aprendiz elige su Dia 1 entre **mañana, pasado mañana o en 3 dias — NUNCA hoy**. La version original del encargo permitia "hoy" como cuarta opcion; se descarto a favor de lo que el backend/frontend viejo ya hacia (decision de producto real, 2026-08-10, documentada en `inicioPrograma.ts` del cliente movil): el reloj del programa avanza a medianoche, asi que firmar de tarde y elegir "hoy" dejaria un Dia 1 de pocas horas, sin tiempo de completar los habitos de esa jornada. El rango valido quedo en **[hoy+1, hoy+3]**, calculado en la zona horaria PROPIA del participante (`participantes_programa.timezone`, nunca UTC ni la del servidor — mismo patron ya usado en `RocaDiariaService`/`HomeAgregadoService`: `clock.now().atZone(zona).toLocalDate()`). **Idempotencia, distincion tecnica y no de negocio:** reactivar con la MISMA fecha que ya esta guardada es un no-op silencioso (200, nada cambia — un timeout de red no puede convertirse en un error); activar con una fecha DISTINTA cuando ya esta activado es `IllegalStateException` (409) — eso si reescribiria la historia del programa. **Que se construyo:** `ParticipacionPrograma.activarPrograma`/`opcionesDeActivacion`/`avanzarDiaDelPrograma` (dominio puro, `users/domain/model/participante`); el campo `diaProgramaAvanzadoEl` (columna `dia_programa_avanzado_el`, ya existia en el baseline para esto y no se mapeaba) da la idempotencia del cron por dia calendario EN LA ZONA DE CADA PARTICIPANTE; `FasePrograma.paraDiaPrograma(int)` nuevo en `users/api` (cortes 1-7/8-34/35-64/65-90, verificados 1:1 contra `phasecontracts.domain.model.contrato.FasePrograma.paraDiaPrograma` y `docs/MODULO_PHASECONTRACTS.md` §1 — sin contradiccion entre docs y codigo, solo la duplicacion de enum ya documentada en D-21); `fijarDia` (ajuste operativo del panel admin) **ahora tambien recalcula la fase** — es la via que dejaba la fase vieja conviviendo con un dia nuevo, la causa mas probable del bug real de arriba. Casos de uso nuevos en `RelojProgramaService` (`users/application/services`, separado de `ParticipacionProgramaService` porque esa clase ya estaba en el techo de 300 lineas de CLAUDE.MD §5.4.8): `ActivateProgramUseCase`, `ConsultarActivacionProgramaUseCase`, `AvanzarDiaProgramaUseCase`. Puerto nuevo `ListarParticipantesConProgramaActivoPort` con paginacion offset/limit real (sin `Pageable` de Spring Data en el puerto, mismo criterio que `ConsultarResumenParticipacionPort.listarAprendices`) para que el cron recorra miles de participantes sin traerlos todos a memoria de una vez. **Endpoints nuevos:** `GET`/`POST /api/v1/onboarding/activate-program` en `ParticipacionProgramaController` — cierran el gap documentado en `docs/GAPS_FRONTEND_BACKEND.md` §7 ("no existe"), aunque el agregado viva en `users` (mismo criterio que `/api/v1/mentor/activate-tracking` en el mismo controller). **Diferencia de contrato deliberada contra el cliente movil real:** `POST` no acepta `startDate` opcional (el viejo backend calculaba "mañana" por default; la regla vigente exige eleccion explicita, sin default) y devuelve 409 en vez de un 200 idempotente con `activated:false` cuando la fecha pedida difiere de la ya activada — documentado para quien actualice el cliente. **Cron:** `AvanzarDiaProgramaScheduler` (`users/infrastructure/adapter/in/scheduler`, 04:50 UTC — antes de `habits.ExpirarRegistrosScheduler` 05:00 y `points.SnapshotRankingScheduler` 05:05, que dependen indirectamente del dia/fase del dia). **Pruebas:** `ParticipacionProgramaTest` (37), `FaseProgramaTest` nuevo en `users/api` (8, bordes exactos 7/8, 34/35, 64/65), `RelojProgramaServiceTest` (9, incluye paginacion con pagina llena de 500), `RelojProgramaIntegrationTest` nuevo (3, Testcontainers, ciclo completo activar→esperar→avanzar→fase, sin mocks), `ParticipacionProgramaControllerTest` (+9, autorizacion negativa 403/401 y contrato 400/404/409 de los 2 endpoints nuevos), `ParticipacionProgramaServiceTest` sin cambios de comportamiento (31). Corridas contra `PermissionEnforcementInterceptor` de D-64 tras rebasear sobre `fde3373`: los endpoints nuevos declaran `Permission.USE_APP` (ya en la matriz de TRAINEE) y pasan sin ajustes | 2026-09-01 | `ParticipacionPrograma.java`; `RelojProgramaService.java`; `AvanzarDiaProgramaScheduler.java`; `docs/MODULO_PHASECONTRACTS.md` §0.2; `docs/GAPS_FRONTEND_BACKEND.md` §7; `docs/ENDPOINTS_FALTANTES.md` |
| D-68 | **ShedLock solo en los schedulers que de verdad lo necesitan, no en todos (C-5, 2026-09-02).** El informe decía "11 schedulers, 4 inseguros"; la verificación uno por uno dio **3 de 10** (el undécimo era una mención en un comentario, no un `@Scheduled`). Los 3 son `evidence.ProcesarColaValidacionScheduler`, `habits.PromoverCambiosHorarioScheduler` y `rag.GenerarInformesSemanalesScheduler`; los otros 7 usan `FOR UPDATE SKIP LOCKED` de vida completa, `INSERT ... ON CONFLICT`, o son barridos idempotentes hacia un estado terminal. **No se aplicó lock "por las dudas"**: cada lock agrega contención y un modo de falla nuevo. **Regla que queda:** todo `@Scheduled` nuevo pasa por la tabla de `docs/informes/auditoria-fixes/C-5.md` y decide; ya se aplicó dos veces en el mismo día, a `EventPublicationMaintenanceScheduler` (C-7) y a `AvanzarDiaProgramaScheduler` (D-67), que nacieron **después** de la clasificación original | 2026-09-02 | `docs/informes/auditoria-fixes/C-5.md`; `V15__shedlock.sql` |
| D-69 | **El outbox de Modulith pasa a `completion-mode: DELETE` con republicación al arranque, y los listeners de `notifications` se vuelven idempotentes (C-7, 2026-09-02).** `UPDATE` (el default) deja la fila para siempre: crecimiento sin límite (E-4). Con `DELETE`, en el camino feliz no queda nada que purgar. **El hallazgo de fondo fue otro:** 4 de los 6 listeners reales no eran idempotentes, y con at-least-once un evento entregado dos veces producía **una notificación y un push duplicados** al aprendiz; se cierra con un índice único parcial sobre el id del evento de origen (`V16`). Se confirmó además que `points` **no consume eventos** — otros módulos lo llaman de forma síncrona | 2026-09-02 | `docs/informes/auditoria-fixes/C-7.md`; `V16__notificaciones_idempotencia_origen_evento.sql` |
| D-70 | **C-18 se cierra sin marcar ningún `@Transactional` nuevo como `readOnly`, y con `open-in-view` apagado (2026-09-02).** La premisa del informe ("solo 8 de 146 son readOnly") medía algo que no era el problema: de los 113 métodos revisados fuera de `habits`, **los 113 escriben**. El patrón del repo es que **una lectura pura no lleva `@Transactional` en absoluto** — se apoya en la transacción por método de Spring Data —, así que todo `@Transactional` que sobrevive es un caso de uso de escritura. Marcar cero es la respuesta correcta, no una omisión. Caso documentado aparte: `pendientesLote` **no escribe pero usa `SELECT ... FOR UPDATE`**, que Postgres rechaza en una transacción de solo lectura — *"¿escribe?"* no alcanza como única pregunta. `open-in-view=false` sí se aplicó, pero **solo tras demostrarlo seguro**: cero relaciones JPA en las 74 entidades, cero carga perezosa, ninguna entidad cruzando fuera de su adaptador | 2026-09-02 | `docs/informes/auditoria-fixes/C-18.md`; `application.yaml` |
| D-71 | **`GET /api/v1/me/cell` devuelve 200 con `{"assigned": false}` en vez de 404 cuando el aprendiz no tiene célula (2026-09-02).** El cuerpo ya era correcto; el status no. Con 404 la app **no puede distinguir "todavía no tenés mentor" de un error real de red o de ruta**, y mostraba un error donde el dueño del producto pidió un mensaje. La inconsistencia era evidente dentro del propio módulo: el endpoint hermano `/me/cell/members` ya devolvía 200 con lista vacía. Se verificó que ningún consumidor dependiera del 404 | 2026-09-02 | `docs/informes/comunidad-mentor-y-tribu.md`; `MiCelulaController` |
| D-72 | **Los tracks del día se generan por DOS vías, no una: barrido nocturno + generación al consultar (2026-09-02).** El hallazgo previo fue que **nunca se generaban**: `RegistroService.generar` existía completo y **nadie lo llamaba**, así que `habit-tracks/today` devolvía `[]` para todos los aprendices. **El argumento que obliga a tener las dos vías es de integridad, no de comodidad:** con solo la generación al consultar, quien nunca abre la app no tendría tracks; sin tracks no hay nada que expirar, sin expiración no se registra ningún fallo, y **su coherencia quedaría intacta en 100** — no abrir la app sería la mejor estrategia para no perder puntos. El barrido corre a las **05:02 UTC** (00:02 en Lima), después del avance del día y **después de la medianoche local**: a las 04:50 UTC en Lima todavía es el día anterior y se regeneraría el día que termina. La generación al consultar filtra por **`hora_limite`**, no por hora de disparo: un hábito sin cierre no vence en el día y siempre entra. **Límite conocido:** un cron a hora UTC fija no cubre bien zonas más al oeste que Perú | 2026-09-02 | `docs/informes/habits-barrido-nocturno.md` |
| D-73 | **"No desactivable" es un campo propio (`habitos.desactivable`), separado de `es_opcional` — decisión del dueño del proyecto (2026-09-02).** Se pidió que cuatro hábitos (audioterapia semanal, pastilla renacer, clase diaria, post diario en comunidad) no se puedan quitar del plan. Se evaluó reusar `es_opcional` y **se descartó con evidencia**: hay 22 hábitos activos con `es_opcional = false` y **uno solo** en `true`, así que los cuatro están marcados igual que los otros dieciocho y el campo no los distingue. Además `es_opcional` significa otra cosa — **si penaliza no completarlo**, una regla de puntaje que se copia a cada `RegistroHabito` — y reinterpretarlo **habría cambiado el puntaje de todos los aprendices** sin que nadie lo pidiera. El default es `true` para que nada existente cambie salvo esos cuatro, y se identifican por `clave_sistema`, no por título (un título se renombra). El campo **no se expone en el DTO de crear hábito personal**: un aprendiz no puede crear un hábito que él mismo no pueda desactivar | 2026-09-02 | `V18__habitos_desactivable.sql`; `docs/AVANCES_2026-09-02.md` |
| D-74 | **El aprendiz elige la hora al crear su hábito personal, y hábito y horario se guardan en la misma transacción — decisión del dueño del proyecto (2026-09-02).** El alta de hábito personal ya existía pero **el hábito nunca aparecía en el día**: los tracks solo se generan para hábitos con un `HorarioHabito` que aplique, y crear horarios era exclusivo de administrador. Se descartaron asignar una hora por defecto y dejarlo sin hora (esto último choca con el diseño, que agrupa la jornada en mañana/tarde/noche). La atomicidad no es un detalle: **un hábito sin horario es exactamente el bug que se está corrigiendo**, así que dejarlo posible por un fallo a mitad de camino sería reintroducirlo. Se agregó al dominio la invariante de que la hora límite sea posterior a la de disparo — sin eso un hábito podía nacer vencido | 2026-09-02 | `docs/informes/habits-personal-con-horario.md` |
| D-75 | **`limitTime` pasa a ser opcional en `PATCH /api/v1/habit-preferences/{habitId}` (2026-09-02).** El DTO lo declaraba `@NotNull` copiando "el contrato HTTP viejo literal" (D-36), pero **19 de los 22 hábitos del catálogo real no tienen hora de cierre**, así que el endpoint devolvía **400 para el 86% de los hábitos**: el aprendiz no podía cambiarle la hora a casi ninguno. No es una relajación del contrato sino una corrección — el contrato viejo describía un cliente que siempre mandaba ambos campos, y el catálogo de hoy no. `triggerTime` sigue obligatoria: sin hora de disparo el hábito no se puede ubicar en la jornada. **Regla que queda:** un DTO copiado de un contrato anterior se contrasta campo por campo contra los datos reales antes de darlo por bueno | 2026-09-02 | E-87; `UpdateHabitPreferenceRequest`; `PreferenciaHorarioService` |

---

## 9. Fuentes verificadas

- [Supabase Docs — Login with Keycloak](https://supabase.com/docs/guides/auth/social-login/auth-keycloak) — Keycloak como proveedor OAuth/OIDC, servidor propio
- [Supabase Docs — Third-party auth overview](https://supabase.com/docs/guides/auth/third-party/overview) — la lista es Clerk, Firebase, Auth0, Cognito, WorkOS; Keycloak **no** está
- [Supabase — JWT Signing Keys](https://supabase.com/features/jwt-signing-keys) y [JSON Web Tokens](https://supabase.com/docs/guides/auth/jwts) — legacy HS256 vs asimétrico RS256/ECC/Ed25519, endpoint JWKS
- [Supabase Docs — Self-hosted auth keys](https://supabase.com/docs/guides/self-hosting/self-hosted-auth-keys)
- [Supabase Blog — Third-party auth](https://supabase.com/blog/third-party-auth-mfa-phone-send-hooks)
- `CLAUDE.MD` §5 (mapeo de dominio), §5.1.2 (carpetas), §5.2.1 (tiempo real), §5.3 (`users`), §5.4 (convenciones), §9 (cuándo separar), §10 (strangler fig)

---

## 9. Accesos directos de la app a Supabase — inventario a eliminar (D-41)

Relevado el 2026-08-24 sobre el repo de la app (`RenaserPlayStoreCopy`) con:

```bash
grep -rhoE '\.from\("[a-z_]+"' --include=*.ts --include=*.tsx .
grep -rhoE '\.rpc\("[a-z_]+"'  --include=*.ts --include=*.tsx .
```

**37 accesos directos a tablas + 2 RPC.** Mientras exista uno solo, la regla de negocio de esa operación vive en el cliente y la única defensa es RLS.

### 9.1 Escrituras — prioridad, son el riesgo real

| Tabla | Op | Archivo de la app | Módulo dueño | Endpoint |
|---|---|---|---|---|
| `leccion_progreso` | upsert | `src/services/cursos.ts:456` | `academy` | ✅ `POST /api/v1/lecciones/{id}/complete` |
| `leccion_progreso` | delete | `src/services/cursos.ts:465` | `academy` | ❌ falta el "descompletar" |
| `onboarding_answers` | upsert | `src/services/onboarding.ts:132` | `onboarding` | ❌ Ola 5 |
| `onboarding_media` | upsert | `src/services/onboarding.ts:503` | `onboarding` | ❌ Ola 5 |
| `onboarding_state` | upsert | `src/services/onboarding.ts:252` | `onboarding` | ❌ Ola 5 |
| `radar_entries` | insert | `src/services/radar.ts:111` | `habits` | ❌ falta |
| `testimonios` | insert | `TestimoniosPanel.tsx:776` | `community` | ✅ `POST /api/v1/testimonios` |
| `trainee_profiles` | upsert | `mentorService.ts:162` | `users` | ❌ falta |
| `trainee_profiles` | delete | `mentorService.ts:209` | `users` | ❌ falta |
| `users` | update | `src/services/users.ts:55` | `users` | ✅ `PATCH /api/v1/users/me` |

### 9.2 Lecturas y RPC

| Origen | Módulo | Endpoint |
|---|---|---|
| `.rpc("catalogo_cursos_bloqueados")` `src/services/cursos.ts:180` | `academy` | ❌ nunca existió en el backend viejo — la app lo llama directo |
| `.rpc("progreso_cursos")` `src/services/cursos.ts:152` | `academy` | ❌ ídem |
| `cursos` ×3, `curso_secciones` ×3, `lecciones` ×4, `leccion_recursos` ×1 | `academy` | ✅ cubiertos por `/cursos`, `/cursos/{id}/secciones`, `/lecciones/{id}` |
| `leccion_progreso` select ×2 | `academy` | ✅ vía el detalle de curso/lección |
| `radar_entries` select ×2 | `habits` | ❌ falta |
| `onboarding_answers` ×2, `onboarding_state` ×1 | `onboarding` | ❌ Ola 5 |
| `mentor_tickets` select | `support` | ✅ `GET /api/v1/tickets` |
| `users` select ×4, `trainee_profiles` select ×2 | `users` | ⚠️ revisar cobertura |
| `testimonios` select | `community` | ✅ `GET /api/v1/testimonios` |
| `routines` select `src/services/routines.ts:8` | — | ⚠️ **código muerto** (ver Anexo A de la auditoría de BD): la tabla no se usa |

### 9.3 Regla permanente

En el **paso 0** de cada módulo (D-33) hay que relevar, además del backend viejo, **qué le pega la app directo a Supabase de las tablas de ese módulo**, y exponer el endpoint equivalente. Un módulo no está terminado mientras su tabla siga siendo escrita desde el cliente.
